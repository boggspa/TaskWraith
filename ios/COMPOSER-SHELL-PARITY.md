# TaskWraith Composer Shell — iOS Parity Inventory & Plan

> **Slice CS1 deliverable** (2026-06-17). Read-only inventory of the desktop `data-composer-style`
> system + the iOS architecture decisions. Part C (per-style recipe specs) was produced by a
> 13-style **deep-read → adversarial-verify** workflow (26 Opus agents, ~1.66M tokens); every
> numeric/color/geometry value is read from the live CSS and skeptic-corrected. Slices CS2–CS8
> reference this document.

**Scope:** iOS parity for desktop **`data-composer-style` only** (NOT the broader
`data-interface-style` app chrome). 13 styles. The `default` shell is **already shipped and
signed off on iOS** — this effort adds the other 12 + their theme handling, plus an iOS picker.
**Do NOT clone the CSS cascade in SwiftUI** — build a pure Swift resolver + recipe system.

---

## Part A — Canonical style list & selection

- **Type:** `ComposerStyle` union — `src/main/store/types.ts:103-153`. Exactly **13**:
  `default, codex, claude, cursor, grok, gemini, kimi, modular, terminal, stub, satellite, obsidian, alabaster`.
  No others. `cursor`/`grok` are **VISUAL-ONLY** shells, independent of the same-named providers.
- **Setting:** `AppSettings.composerStyle` (`types.ts:1381`), default `'default'` (`store/index.ts:392`).
  **Global, not per-chat.**
- **Applied to DOM:** `src/renderer/src/hooks/useAppearance.ts:227-320` (`applyToDocument`) sets BOTH
  `data-composer-style` and `data-interface-style` on `<html>` (`:257-258`). Every composer
  descendant matches → the shell is global.
- **Three sync'd lists:** type union (`types.ts:103`), `COMPOSER_STYLE_OPTIONS`
  (`SettingsPanel.tsx:462-548`), `SIDEBAR_COMPOSER_STYLE_OPTIONS` (`Sidebar.tsx:316-330`).
  Picker = SettingsPanel `<select>` (`:2469-2491`) + Sidebar quick-switcher.
- **Send-glyph mapping** (`App.tsx:20675-20685`): `claude`→ClaudeReturn (return); `codex`/`gemini`/
  `cursor`/`grok`/`kimi`→ArrowUp; **all others**→RunSymbol (triangle).

---

## Part B — Composer DOM structure (`App.tsx:17931-20764`)

```
.composer-area  (interface-${style})
├─ welcome-hero / welcome-hero-ensemble        (welcome only)
├─ ABOVE-BAR REGION  (gated)
│   ├─ float-above (cursor + codex ONLY):       App.tsx:18185-18186 aboveRowsFloatAboveStack
│   │     primaryWorkspaceAboveBar + externalWorkspaceAboveRows  (rendered DETACHED above stack)
│   └─ .composer-above-bar-stack
│       ├─ .composer-above-bar.style-unified    (pills: git · changes · action[Create PR])
│       ├─ EnsembleRosterPresetPicker (compact) · EnsembleParticipantsAboveRow · QueuedMessagesAboveRow
├─ .composer-surface   [drag handlers]
│   ├─ .composer-chips                          (warnings, OllamaHealthChip, top-toggles, attach tray)
│   ├─ .composer-inner-module
│   │   ├─ .composer-textarea-wrap              (HighlightOverlay + textarea.composer-textarea)
│   │   └─ .composer-bottom-controls
│   │       └─ .composer-control-footer
│   │           ├─ .composer-inline-pickers     [data-composer-control = attach|permission|ensemble-mode|provider|model]
│   │           └─ .composer-inline-actions > .composer-send-cluster > .composer-action-btn.run-btn / .stop-btn
│   └─ .composer-telemetry-row                  (run/cumulative timecodes · screen-watch · workspace switcher · token tally)
```

- **Default control order** (`07-composer-shells.css:94-99`): attach · permission · ensemble · provider · workspace · model (model `margin-left:auto`).
- `.composer-bottom-controls` = `display:contents` for **11** shells; a real lit rect ONLY for **obsidian/alabaster** (two-surface split).
- Side-chat composer is a parallel `side-chat-*` structure (`App.tsx:21374-21601`), also `interface-${style}`.
- CSS shard ownership: **03** default native surface + merged-instrument `:has()` frame; **05** all-13 aura matrix; **07** codex/claude master + gemini/kimi/cursor base; **08** modular/terminal/stub/satellite/obsidian/alabaster master + two-surface split; **10** grok/cursor master + gemini capsule + kimi; **04** settings-preview only.

## Part C — Per-style recipe specs (verified)

### default — TaskWraith native
**Essence:** Two-layer "instrument" composer and the shipped iOS baseline: a polarity-neutral liquid-glass smoked bezel wrapping a hard-cornered, opaque near-black/near-white typing panel. Flat in transcript; tinted side-glow only on the welcome surface.
**Material:** Two materials. OUTER `.composer-surface` = translucent glass: `--native-instrument-glass-bg` = `color-mix(in srgb, var(--surface-2) 46%, transparent)` DARK / `58%` LIGHT; `backdrop-filter: blur(20px) saturate(155%)`. INNER `.composer-inner-module` = OPAQUE solid `--native-instrument-inner-bg` hardcoded **#141418** (dark) / **#f0f0f3** (light), NOT theme-tinted; gets extra `blur(6px)` on glass/theme prompt-surface variants. Solid fallbacks: outer → `--native-instrument-outer-solid` = `var(--surface-2)` (#24242a dark / #f1f1f4 light); inner → `--native-instrument-inner-bg-solid` = #141418 / #f0f0f3. Glass dropped (→solid, blur→none) under `[data-prompt-surface="solid"]`, `[data-reduce-transparency="true"]`, `@supports not backdrop-filter`, AND macOS `[data-appearance="native_glass"]`. Base un-skinned `--composer-bg` = `rgba(7,16,36,0.92)` dark / `rgba(255,255,255,0.78)` light.
**Palette:** surfaceFill OUTER glass `color-mix(surface-2 46%/58% transparent)` (surface-2 #24242a D / #f1f1f4 L), solid fallback surface-2 · innerModuleFill **#141418 D / #f0f0f3 L** (hardcoded card-in-card) · border `--native-instrument-glass-border` = `color-mix(text-primary 11%/9% transparent)`; dividers text-primary 12%(stack)/11%(module) · focusAccent base `:focus-within` border `rgba(126,197,255,0.58)` + rings `0 0 0 1px rgba(126,197,255,0.18), 0 0 22px rgba(82,170,255,0.20), 0 0 42px rgba(82,170,255,0.12)` (gemini-provider variant border `rgba(105,157,255,0.56)`) · textPrimary `var(--text-primary)` = `rgba(255,255,255,0.92)` D / `rgba(18,21,27,0.92)` L · placeholder `color-mix(text-secondary 60%, transparent)`; **text-secondary = rgba(255,255,255,0.55) D / rgba(18,21,27,0.62) L** [corrected: light is 0.62, not 0.55] · rimOrGlow OUTER `inset 0 0 0 1px outer-rim + inset 0 0 14px outer-rim-glow + inset 0 1px 0 (text-primary 8%/7%) + 0 18px 46px color-mix(#000 20%/14%)`; INNER provider rim `inset 0 0 0 1px color-mix(provider-color 13%, transparent)` (base neutral 4%)
**Geometry:** surfaceRadius `calc(--radius-lg * 2.45)` = 14×2.45 = **34.3px** (welcome ×2.35 = 32.9px; compact radius-lg 7px → 17.15px) · innerRadius **0px** (square, full-bleed, clips to surface overflow:hidden) · controlShape capsule(999px) for send + action btns; inner chips 100px pill · sendButton **40×40px circle (999px)**, armed fill `color-mix(--success 18%, transparent)` over `1px color-mix(--success 45%)` + inset `1px --success 18%`; glyph color `var(--success)` = **#4cc38a**; glyph 28×28 run-triangle; stop `--danger #e54d4d` 14%/40%; queue `--accent #5a8cff`; hover translateY(-1px)+scale1.06 · spacing surface padding 14px 14px 8px (default re-skin bottom→10px; welcome 16/16/10); inner padding 10px 10px 6px gap 8px; inline-pickers gap 7px 10px justify space-between
**Controls:** `.composer-control-footer > .composer-inline-pickers + .composer-inline-actions > .composer-send-cluster`. Cluster is `display:contents` (no lift). Order: attach(0) permission(1) ensemble-mode(2) provider(3) workspace(4) model(5, margin-left:auto). Plain inline pill controls, transparent resting chrome, no ::after carets. **Running-state exception [corrected]:** when a Stop button is present, `[data-composer-style="default"] .composer-inline-pickers:has(.composer-inline-actions .stop-btn)` gets `flex-wrap:wrap; row-gap:4px` and the action cluster gets `width:100%; order:-1; justify-content:flex-end; margin:0` — a default-only second action row that appears ONLY while a turn is actively running (model this state in SwiftUI). Secondary `.composer-telemetry-row` (timecodes + workspace switcher + token tally) sits below on the glass padding.
**Send glyph:** run-triangle
**Typography:** Sans. Textarea `var(--composer-font-family)`=`var(--font-sans)`; size `calc(--font-size-md + 2px)` ≈ 21.1px; line-height 1.55; field-sizing:content min `calc(1.55em+22px)` → max `calc(1.55em*3+22px)`. Placeholder dimmed, NOT italic. Chips `--font-size-xs` ≈ 15px. No mono/serif/tabular-nums.
**Row policy:** MERGED-INSTRUMENT via `:has()`. No above-rows → standalone surface, full 34.3px on all corners. `.composer-area:has(.composer-above-bar-stack)` → merge engages (scoped `.app-transcript`): stack becomes rounded TOP (34.3/34.3/0/0, glass fill, gap:0, border-bottom:none), rows go transparent/borderless flush segments, adjacent segments get 1px text-primary-12% top divider, first child top re-rounded 34.3px, surface pulls flush (margin-top:-space-sm), squares TOP corners, 1px top divider seam → rounded BOTTOM of one instrument. Standalone above-rows carry the same glass fill/border/shadow.
**Effects:** OUTER `::before` side-vignette (per-provider tinted) — KILLED in chats (`background:none!important;box-shadow:none!important`), shows only on welcome. INNER `::before` ALWAYS-ON diagonal sheen `linear-gradient(135deg, text-primary 8% @0%, transparent 46%, text-primary 3% @100%)`. INNER `::after` provider RIM `inset 0 0 0 1px color-mix(provider-color 13%, transparent)` (neutral 4%), steps aside under `.fx-agent-aura`, 160ms cross-fade. OUTER `::after` running-pulse/provider rim stays on surface only (base default provider rim at 03:2956; running pulse 05-fx:2935-2956); stack `::after` force-removed. Merged hairline dividers; inner inset seam (margin-bottom 6px + border-bottom 1px text-primary 11%); fx-agent-aura neutralized to neutral glass on stack, colored aura survives on surface.
**Theme behavior:** THEME-DERIVED (not immune). Reads surface-2/text-primary for bezel+rims, re-tints per theme. Explicit light/dark flip: base block sets dark vars; `:is([data-theme=light],citrus,mist,sage,alabaster)` flips to WHITE glass (inner #f0f0f3, glass surface-2 58%, softer rims, 0 18px 46px #000 14%). Inner fill is the near-constant. Visual FALLBACK shell for unknown styles.
**A11y fallbacks:** reduce-transparency / `[data-prompt-surface="solid"]` → drop glass to solid surface-2 + inner solid #141418/#f0f0f3 no blur; `@supports not backdrop-filter` same. macOS native_glass + reduce-transparency=false → `backdrop-filter:none` but keeps translucent color. reduce-motion → inner box-shadow/seam transition none + provider-rim transition none; pulse/aura removed for merged default. No default-specific high-contrast rule (inherits global).
**Skeptic:** accurate=false; corrections applied = (1) running-state second action row added (03:3873-3885); (2) placeholder light text-secondary 0.62 not 0.55 (theme.css:274). Send glyph run-triangle matches global facts. Confidence: high — both corrections grounded in exact file:line; bulk of spec verified accurate.
**Evidence:** `src/renderer/src/assets/css/03-composer-welcome-activity.css:3103-3141` (native-instrument vars dark+light), `:3145-3177` (outer glass + fallbacks), `:3188-3228` (kill vignette / re-skin above-rows), `:3240-3335` (merged :has() frame), `:3379-3497` (inner module + readability), `:3532-3554` (textarea), `:3873-3885` (running-state second row), `:6324-6398` (action-btn / run glyph 28px); `07-composer-shells.css:94-99` (control order), `:3766-3819` (inner ::after provider rim); `10-provider-shell-overrides.css:3380-3446` (merged reinforce); `theme.css:109,274,161` (text-secondary D/L, success); `App.tsx:20675-20685` (RunSymbolIcon).

### codex — Codex
**Essence:** Two-surface dark "card-in-card": near-black rounded-22px shell (#212121) holding a slightly-lighter inner module (#252525) with top corners echoing the shell and tighter bottom corners. All chrome stripped to floating satellite text-tokens with ▾ carets; the hallmark is a solid light send circle with a dark up-arrow.
**Material:** Solid, opaque — NOT glass. Outer `.composer-surface` solid **#212121** (07:26); inner `.composer-inner-module` solid **#252525** (07:40). No backdrop-filter; `[data-reduce-transparency="true"]` forces `backdrop-filter:none` (02:2963-2966). Light-theme (light/mist/sage): surface → `var(--surface-2)` bg, `var(--surface-border)`, `var(--surface-text)`, shadow `0 4px 18px rgba(0,0,0,0.08)` (08:3971-3979). Inner module carries `inset 0 -1px 0 color-mix(#ffffff 7%, transparent)` (a BOTTOM-edge inner highlight [corrected — `inset 0 -1px 0` paints the bottom edge, not the top]) + drop `0 8px 16px rgba(0,0,0,0.12)`.
**Palette:** surfaceFill **#212121** (outer) / light `var(--surface-2)` · innerModuleFill **#252525** (distinct card-in-card) · border `1px solid transparent` (forced transparent incl :focus-within, 07:64-73); light `var(--surface-border)` · focusAccent none — focus deepens shadow to `0 6px 24px rgba(0,0,0,0.46)`, border stays transparent; light focus border `color-mix(--surface-border 44%, --surface-text 18%)` shadow `0 4px 22px rgba(0,0,0,0.12)` · textPrimary model token `color-mix(#ffffff 88%, transparent)`; telemetry `color-mix(#ffffff 58%)` · placeholder `color-mix(#ffffff 38%, transparent)`; light `var(--text-tertiary)` · rimOrGlow outer rim REMOVED; resting shadow `0 6px 24px rgba(0,0,0,0.4)`; agent-aura (on `.composer-above-bar-stack` rows, orange) `0 0 0 1px rgb(255 154 85 /0.28) + 0 18px 58px /0.18` at rest, `/0.5 + 0 22px 68px /0.24` running; codex aura RGB `255 154 85 / 255 218 112`
**Geometry:** surfaceRadius **22px** outer; inner **21px 21px 14px 14px** · controlShape mixed: + attach & ensemble-mode r6, provider & workspace r6, permission & model r999 (transparent — pill only on hover) · sendButton **circle r999, 32×32px**, fill `color-mix(#ffffff 86%, transparent)` → solid #ffffff hover, glyph **#0d0d0d**, no border/shadow (interface-style variant fill white 90% !important); disabled fill white 12% glyph white 36%; stop/queue 32px r999 · spacing inner padding 14px 14px 8px, column gap `--space-sm`; textarea padding 6px 6px 12px, min-height 58px; telemetry min-height 40px padding 7px 16px 10px; footer/picker strips forced transparent so controls float
**Controls:** Order via `order`: attach 0, permission 1, ensemble-mode 2, provider 3, workspace 4, model 5 (margin-left:auto). All naked transparent text-tokens with faint hover surface. + attach 28×28 r6 white 60%→hover bg white 8%. Permission token min-height 28 r999 transparent, color **#ff7a59** (warning orange) + ▾ 9px opacity 0.8. Ensemble-mode: container stripped, two labels r6 pad 2px 8px, is-active bg `color-mix(#8c80ff 18%, transparent)` weight 600 — the ONLY purple in the shell. Provider/workspace tokens r6 transparent white 70%, icon 11×11 opacity .78, ▾ 9px. Model token r999 transparent white 88% weight 500, svg hidden, ▾ 9px. Telemetry: workspace-button margin-left 0, token-tally margin-left auto. Dormant `.composer-codex-footer` chip row in CSS (07:352-379) but NEVER rendered (no JSX ref).
**Send glyph:** arrow-up
**Typography:** System sans. Textarea `calc(--font-size-md - 1px)`. Satellite tokens `calc(--font-size-xs + 1px)` weight 500 (600 active). Carets ▾ at 9px. No italic placeholder, no tabular-nums.
**Row policy:** Above-bar git/Create-PR + secondary-workspace rows FLOAT ABOVE the merged stack as detached pills (`aboveRowsFloatAboveStack = composerStyle==='cursor' || 'codex'`, App.tsx:18185-18186). Aura INVERTED vs most: codex excluded from generic group, matched by `.composer-above-bar-stack:not(:has(.ensemble-above-row)).fx-agent-aura` — aura renders on the floated above-row ONLY when NO ensemble row present; suppressed when an ensemble row IS present. Above-rows = relative-positioned pills with inset ::after rim.
**Effects:** ▾ chevron ::after on permission/provider/workspace/model (9px, opacity ~0.7-0.8). Conditional lightning-bolt ::before on model token gated `[data-fast-mode-active="true"]` (22×24 masked SVG, bg `color-mix(--accent 90%, #ffffff)`). Inner-module bottom-edge inset highlight (07:44). **Two-surface card-in-card split** (defining structural effect). Agent-aura rim ::after on floated above-row (orange), opt-in, suppressed when ensemble row present. Hover-only faint white 6-8% surfaces. Send button NO pseudo-elements. Dormant codex-footer CSS produces no visible effect.
**Theme behavior:** Hybrid. Dark themes LOCK own palette (#212121/#252525/#ff7a59), force outer rim transparent even on focus. Light themes (light/mist/sage) THEME-DERIVE: surface → surface-2/border/text, textarea → surface-text, placeholder → text-tertiary, above-bar pills re-key dark-on-light `rgba(29,29,31,0.72)`. interface-style=codex lifts send fill white 90% !important. Obsidian inverts welcome cards to light gradient.
**A11y fallbacks:** reduce-motion → codex above-row aura pulse killed (05:3272-3284); shell control transitions dropped. reduce-transparency → forces `backdrop-filter:none` (minimal — surface already solid). No codex-specific forced-colors; relies on fill-delta (#212121 vs #252525) — SwiftUI should add explicit borders for high-contrast.
**Skeptic:** accurate=false; corrections applied = inner-module inset highlight is BOTTOM-edge (`inset 0 -1px 0`), not top (07:44). Send glyph arrow-up matches global facts. Confidence: high.
**Evidence:** `07-composer-shells.css:22-29` (outer #212121 r22), `:30-45` (inner #252525 r21/21/14/14 + insets), `:60-73` (focus + rim removed), `:107-118` (order), `:200-226` (ensemble #8c80ff purple), `:247-268` (permission #ff7a59 + ▾), `:271-321` (model + lightning ::before), `:323-350` (send light circle 32px, glyph #0d0d0d); `App.tsx:18185-18186` (float above), `:20677-20684` (ArrowUpSendIcon); `AppChromeSymbols.tsx:439-455` (ArrowUpSendIcon); `05-polish-fx-layouts.css:2634-2637` (aura orange), `:3027-3088` (codex `:not(:has(.ensemble-above-row))`); `08-theme-picker-overrides.css:3971-4047` (light derive).

### claude — Claude
**Essence:** Whisper-quiet near-monochrome dark shell mimicking Claude Code's terminal composer — a single rounded-rect bubble (the textarea) with chrome-less floating-satellite footer controls and a matched dark header band above. Tiny squared send button with a return-arrow glyph nudged up 9px.
**Material:** Translucent-ish dark; the bubble chrome lives on `.composer-textarea`, not the surface (composerStyle and interfaceStyle always carry the same value). Interface-style rules (07:3344-3360) STRIP the surface to transparent/no-border/no-shadow/no-blur and re-home chrome onto the textarea. **Rendered bubble = `.composer-textarea`:** background **#1e1e1e !important** [corrected — 08:7801-7804 overrides 07's #151515 by source order] (light/mist/sage **#f0f0f3**, 08:7806-7809); border **rgba(255,255,255,0.18) !important** [corrected, 08:7803] (light rgba(0,0,0,0.14)); border-radius 14px; box-shadow `0 4px 20px rgba(0,0,0,0.32)`; padding 18px 14px. `backdrop-filter` removed on surface; reduce-transparency kills any surface backdrop-filter.
**Palette:** surfaceFill bubble **#1e1e1e** D / **#f0f0f3** L [corrected from #151515]; bare composer-style fallback `color-mix(#161616 94%, transparent)`; provider-scoped `--composer-bg: color-mix(#262626 92%, transparent)` · innerModuleFill **n/a** (footer chrome forced transparent !important) · border bubble **rgba(255,255,255,0.18)** [corrected]; chips-bar hairline (DEAD — see effects) · focusAccent **rendered focus-within textarea border rgba(255,255,255,0.30) !important; box-shadow `0 4px 22px rgba(0,0,0,0.36), 0 0 0 1px rgba(255,255,255,0.18) !important`** [corrected, 08:7811-7817 — 30% not 26%]; send-button focus ring `0 -9px 0 2px var(--focus-ring)` offset up 9px · textPrimary `color-mix(#ffffff 70%)` for model/provider/workspace/run-glyph · placeholder `color-mix(#ffffff 30%, transparent)` · rimOrGlow bubble drop `0 4px 20px rgba(0,0,0,0.32)`; surface ::before force-hidden; above-bar-stack per-row agent-aura `0 0 0 1px rgb(var(--agent-aura-rgb,87 148 255)/0.28), 0 18px 58px /0.18`, running /0.5 + /0.24 pulse 2.8s
**Geometry:** surfaceRadius **14px on the textarea bubble** (surface transparent in production) · controlShape mixed: permission = **contained rounded-rect button** [corrected from plain text] bg `rgba(255,255,255,0.04)`, border `1px rgba(255,255,255,0.10)`, radius `var(--radius-md,8px)`, padding 3px 10px, color **rgba(255,255,255,0.92)** (08:7900-7906); model = plain text white 70% svg hidden; attach = naked glyph 26×26 radius0; provider/workspace/ensemble-mode = capsule 999px min-h 24px 1px white 14% border · sendButton **28×28, border-radius 8px (squared)**, bg transparent, border 0, glyph `color-mix(#ffffff 58%)`; run-btn glyph white 70% (hover #ffffff, disabled white 22%); interface-style run glyph tint `color-mix(#ffb36c 82%, #ffffff 18%) !important`; glyph `translateY(-9px) !important` (hover scale1.06) · spacing surface padding 4px 12px 8px; **textarea min-height 56px** [corrected — 07:1879 overrides the 48px at 07:439]; above-bar gap 10px; send/stop/steer abs-positioned `bottom:82px right:14px !important` (08:7831-7837)
**Controls:** Order permission(0) attach(1) ensemble-mode(2) provider(3) workspace(4) model(5, margin-left:auto). Permission is a **contained button** (rgba(255,255,255,0.04) bg, rgba(255,255,255,0.10) border, radius-md, color 92% — NOT plain text, NO orange accent) [corrected]. Model plain text white 70%. Attach "+" 26×26 transparent no-border radius0, hover changes COLOR only; light themes var(--text-primary). Provider/workspace matching low-key 999px outlined pills (white 14% border, white 70% text, 11px icon opacity .78). Ensemble-mode 999px pill, active quiet white tint (white 12% bg, weight 600), NO orange. Footer chrome force-transparent. Telemetry row border-top 0 near-flush. A claude-styled combined model+reasoning picker exists (`.composer-combined-picker-trigger`, 08:8638-8651) — if rendered it replaces the native model select.
**Send glyph:** return
**Typography:** System sans. Textarea `calc(--font-size-sm + 1px)`; permission/model/provider/workspace/ensemble-mode `calc(--font-size-xs + 1px)`; weights 500 (ensemble active 600). Placeholder NOT italic (white 30%). **tabular-nums IS declared** on above-bar files + stats chips (08:7849, 7858) [corrected from "none"].
**Row policy:** Above-bar git/branch/changes + ensemble/queued rows in `.composer-above-bar-stack`. Stack treated as pure gap wrapper; when `.fx-agent-aura`, stack box-shadow + ::after suppressed and rim fanned to EACH child row via per-row ::after (claude grouped with gemini/kimi/modular/satellite/stub/obsidian/alabaster/terminal). Escalates on running (pulse), approval (warn), failed (danger). Above-bar re-skinned to matched dark header band #151515 / 1px white 9% / 14px radius / `0 4px 20px` shadow / padding 8px 14px !important (07:3267-3273). Send/stop/steer abs-positioned bottom:82px right:14px.
**Effects:** Surface ::before vignette force-DISABLED. `.composer-chips` is **display:none** (07:1871-1872) — the chips-bar styling incl its hairline is DEAD CSS [corrected]; actual header band is `.composer-above-bar`. Above-bar files + stats chips are **SOLID gray #3a3a3a !important** (border 1px rgba(255,255,255,0.08), radius-md 10px, padding 4px 10px, color 92%; 08:7843-7860) [corrected — not transparent hairline]; branch + summary remain transparent at 78% (08:7259-7268) → row is a MIX. Create-PR/above-bar-action also solid gray #3a3a3a (08:7302-7311; hover #444444, disabled rgba(58,58,58,0.6)). Per-row agent-aura ::after. Send-glyph translateY(-9px) with focus ring shifted up 9px. No paper/perforation/caret/two-surface split. Context wheel **#5d9eff (cyan-blue)** (08:7927-7928 overrides 07's amber) [omission noted].
**Theme behavior:** Largely theme-IMMUNE / self-locking in dark contexts (white-on-#1e1e1e/#262626 literals). Theme-conditional only: (1) light/mist/sage recolor the "+" glyph to var(--text-primary) mixes; (2) light/mist/sage + obsidian restyle welcome starter cards. No provider-color theming of the surface. Single warm accent = run-glyph orange tint `color-mix(#ffb36c 82%, #ffffff 18%)`. Light bubble #f0f0f3 (08:7806).
**A11y fallbacks:** reduce-motion → per-row aura pulse killed (05:3247-3284) + globally; action-btn hover transform zeroed; send-confirming glyph anim stopped. reduce-transparency → surface backdrop removed (already stripped). No claude-specific high-contrast (global fallback); focus via box-shadow rings.
**Skeptic:** accurate=false; corrections applied = (1) bubble fill **#1e1e1e**/#f0f0f3 not #151515 (08:7801-7809); (2) bubble border **rgba(255,255,255,0.18)** not white 10% (08:7803); (3) permission = **contained rounded-rect button** white 92%, NOT plain text (08:7900-7906); (4) **NO orange permission accent** anywhere — neutral white (orange is run-glyph only); (5) textarea min-height **56px** not 48px (07:1879); (6) `.composer-chips` header band is **display:none / dead CSS** (07:1871); (7) above-bar files/stats chips **solid #3a3a3a**, Create-PR solid #3a3a3a (08:7843-7860, 7302); (8) tabular-nums **IS** declared (08:7849); (9) focus border **30%** + differing shadow stack (08:7811). Send glyph return matches global facts. **The entire 08-theme-picker-overrides.css claude layer (~70 rules) loads after 07 and is the last-writer — double-check this style.** Confidence: high.
**Evidence:** `07-composer-shells.css:400-410, 437-444, 456-466, 521-545, 636-705` (base shell, supplanted by 08), `:1871-1873` (chips display:none), `:1874-1881` (min-height 56px), `:3322-3360` (surface→textarea chrome), `:3409-3411` (run orange tint); `08-theme-picker-overrides.css:7801-7817` (bubble fill/border/focus), `:7831-7837` (reanchor), `:7843-7860` (files/stats solid #3a3a3a + tabular-nums), `:7900-7922` (permission contained button), `:7927-7928` (context wheel cyan); `05-polish-fx-layouts.css:3011-3284` (per-row aura); `AppChromeSymbols.tsx:418-434` (ClaudeReturnSymbolIcon); `App.tsx:20675-20685`.

### gemini — Gemini
**Essence:** "Single rounded pill" composer — the surface is fully transparent (flex column only); the entire visual chrome is a deep-navy gradient CAPSULE (26px) holding only the textarea. The [+], model pill, and send button are CSS-anchor-positioned onto the capsule's bottom-right edge; permission · ensemble · provider · workspace flow below as a transparent satellite row. Signature Gemini blue #5a8cff.
**Material:** Two-surface split. OUTER `.composer-surface` fully transparent: bg/border/box-shadow/backdrop-filter all `transparent`/`none` !important, padding 0, radius 0, flex column gap 6px, overflow:visible (07:2152-2165); ::before killed (content:none). INNER capsule `.composer-textarea-wrap` SOLID gradient, NO blur: dark `linear-gradient(180deg,#141d30 0%,#0d1320 100%)`. Light (light/mist/sage AND alabaster) `linear-gradient(180deg,#ffffff 0%,#eef3ff 100%)`. Obsidian keeps dark navy (capsule surface forced transparent at 10:746-751). Opaque capsule → reduce-transparency rules inert.
**Palette:** surfaceFill CAPSULE dark `linear-gradient(180deg,#141d30,#0d1320)`; outer surface transparent; light/alabaster `linear-gradient(180deg,#ffffff,#eef3ff)` · innerModuleFill **n/a** (capsule is the only filled surface) · border capsule `1px color-mix(#5a8cff 22%, transparent)`; light `color-mix(#5a8cff 30%, var(--surface-border))` · focusAccent **#5a8cff**; focus border `color-mix(#5a8cff 58%)`; light `60%` · textPrimary dark white (placeholder `color-mix(#ffffff 38%)`); light `var(--surface-text)` (placeholder `color-mix(#1d1d1f 42%)`); obsidian `rgba(246,249,255,0.94)`; alabaster `rgba(18,21,27,0.92)` · placeholder dark `color-mix(#ffffff 38%)` / light `color-mix(#1d1d1f 42%)` · rimOrGlow capsule `0 6px 30px rgba(0,0,0,0.42)`; focus `0 6px 34px rgba(0,0,0,0.46), 0 0 0 4px color-mix(#5a8cff 12%)`; light focus `0 4px 22px rgba(20,40,90,0.14), 0 0 0 4px color-mix(#5a8cff 14%)`. Model pill bg `color-mix(#5a8cff 18%)` text `color-mix(#b8ccff 95%)`. Send circle bg `color-mix(#ffffff 90%)` glyph **#0e1218**
**Geometry:** surfaceRadius outer 0; CAPSULE **26px** min-height 50px padding 3px 0; single-line `--composer-capsule-single-h = calc(1.55*--font-size-md + 30px)` · controlShape capsule/999px every chip (model/provider/workspace/ensemble-mode); permission plain text; attach borderless 999px · innerRadius n/a · sendButton **circle 32×32px (999px)**, fill `color-mix(#ffffff 90%)` → #ffffff hover, glyph **#0e1218**, border 0, box-shadow none; disabled fill white 12% glyph white 36%; arrow-up glyph 28px · spacing textarea gutters padding `9px 224px 9px 46px` (46px clears [+], 224px clears [model][send]); satellite pickers padding 0 6px gap 6px; lift offsets: attach top=anchor(bottom)−single_h+12px left:10px; model +13px right:50px max-width 172px; send-cluster +12px right:12px
**Controls:** SPLIT by lift. Footer dissolved (`display:contents`). Three controls position:absolute via CSS anchor `--composer-capsule`: attach bottom-left, model pill bottom-right-inner, send-cluster bottom-right. Remaining flow in transparent satellite row: permission(1) · ensemble-mode(2) · provider(3) · workspace(4). Model pill = filled blue tint capsule (#5a8cff 18%) ellipsized; provider/workspace/ensemble get blue tint via :is() (#5a8cff 18% bg, transparent border, #b8ccff text) over neutral outline-pill base. Permission plain text (svg hidden). Attach large borderless "+" (color-only hover). Ensemble-mode 999px segmented (active #5a8cff 22% bg weight 600). Telemetry below, transparent.
**Send glyph:** arrow-up
**Typography:** Sans. Textarea `var(--font-size-md)`; provider/workspace/ensemble `--font-size-xs`; model + permission `calc(--font-size-xs + 1px)`; weights permission/model/ensemble 500 (active 600). No italic placeholder, no tabular-nums, no letter-spacing.
**Row policy:** Detached-pill family. `.composer-above-bar-stack` is gap wrapper only; aura must NOT paint at stack level — gemini explicitly in the :is() list that nulls stack box-shadow/::after and re-homes aura to EACH child row via per-row ::after. No `:has()` row-count gating for gemini (always fans per-row). Lift uses `anchor()` math; single-vs-multi-line handled by `anchor(--composer-capsule bottom)`.
**Effects:** OUTER ::before KILLED (content:none). CAPSULE focus-within → border #5a8cff 58% + glow `0 6px 34px` black + 4px #5a8cff-12% ring (signature blue halo). Per-ROW agent-aura ::after (not on capsule) idle `0 0 0 1px rgb(.../0.28) + 0 18px 58px /0.18`; running `/0.5 + 0 22px 68px /0.24` + pulse 2.8s; approval warn `/0.5` no anim; failed danger `/0.42` no anim. Lifted controls abs-positioned via `position-anchor:--composer-capsule` (load-bearing structural effect). Send button NO inset ring / NO box-shadow (flat). Attach hover color-only (transform:none).
**Theme behavior:** Largely THEME-IMMUNE at capsule level (locks navy by default; obsidian+alabaster gemini chrome intentionally dark navy). Dark default applies under system/dark/obsidian, base for all themes unless repainted. Flips: (1) light/mist/sage repaint capsule near-white + light focus, surface stays transparent; (2) alabaster repaints near-white + re-keys satellite chips to #23345f-on-#5a8cff-10% + above-rows var(--surface-2) #5a8cff-32% border; (3) obsidian KEEPS navy (above-rows radial+linear navy, text rgba(246,249,255,*)). interface-style=gemini darkens app chrome (sidebar/main-pane `color-mix(#0a1020 92%/88%)`).
**A11y fallbacks:** reduce-motion → running aura pulse killed AND running rim hidden (05:3247-3284); capsule border/shadow transitions not disabled (focus glow still cross-fades). reduce-transparency → NO-OP (surface already transparent; opaque capsule has no backdrop-filter). No gemini-specific high-contrast (always-on solid capsule + opaque chips meet contrast).
**Skeptic:** accurate=true; corrections applied = none. Minor non-material citation nits only (reduce-transparency rule scoping, App.tsx line ±, an additional 07:3505+ interface-style chrome block consistent with the spec). Send glyph arrow-up matches global facts. Confidence: high.
**Evidence:** `07-composer-shells.css:2131-2139` (capsule tokens), `:2152-2184` (surface invisible + single-h), `:2188-2210` (capsule 26px + anchor-name + focus), `:2211-2223` (gutters 9px 224px 9px 46px), `:2276-2301` (LIFT abs+anchor), `:2330-2392` (attach/model/permission), `:2480-2545` (provider/ensemble/send); `05-polish-fx-layouts.css:3011-3284` (per-row aura); `08-theme-picker-overrides.css:4408-4425` (light capsule); `10-provider-shell-overrides.css:691-751` (obsidian), `:3249-3363` (alabaster), `:3009-3012` (chrome darken); `App.tsx:20677-20682` (ArrowUpSendIcon).

### kimi — Kimi
**Essence:** Single solid near-black rounded rectangle (no glass, no card-in-card) with a green-yellow/olive (#a9bd55) accent surfacing only on focus and hover/active. Textarea sits directly on the surface; footer is a flat strip of mostly-transparent outlined pills; send button is a 32px filled circle whose hover flips to olive.
**Material:** Solid, opaque — NOT glass. Base fill `color-mix(#0a0a0c 96%, transparent)` ≈ `rgba(10,10,12,0.96)` (07:2577). No backdrop-filter ever; reduce-transparency swaps are no-ops. Base blue edge-gradient ::before suppressed (display:none). Light (light/mist/sage/alabaster) `var(--unified-soft-surface-bg)`. Obsidian forces solid **#202124** surface / **#1f2023** above-bar. Ensemble dark contexts force surface #202124, above-bar #1f2023.
**Palette:** surfaceFill `rgba(10,10,12,0.96)`; light `var(--unified-soft-surface-bg)`; obsidian #202124 / above-bar #1f2023 · innerModuleFill **same as surface** (no inner card) · border `1px color-mix(#ffffff 8%)` ≈ rgba(255,255,255,0.08); light `var(--unified-soft-surface-border)`; obsidian rgba(255,255,255,0.12) · focusAccent **#a9bd55** — focus border `color-mix(#a9bd55 50%)` + 3px ring `color-mix(#a9bd55 10%)`; light darker olive **#84a33b** 42% border + 3px #84a33b 12% ring · textPrimary model #ffffff 88%, permission #ffffff 80%, provider/workspace/ensemble #ffffff 78%; light var(--text-primary) / #34410f chip text · placeholder `color-mix(#ffffff 36%)` ≈ rgba(255,255,255,0.36); light `var(--text-tertiary)` · rimOrGlow no rest rim; rest shadow `0 4px 16px rgba(0,0,0,0.5)`; focus adds `0 0 0 3px color-mix(#a9bd55 10%)` + deepens shadow to `0 4px 16px rgba(0,0,0,0.55)`
**Geometry:** surfaceRadius **18px** (above-bar 18px) · controlShape capsule(999px) provider/workspace/model/ensemble/image-picker; permission plain text padding 0 6px · sendButton **32px circle (999px)**, fill `color-mix(#ffffff 18%)` ≈ rgba(255,255,255,0.18), no border, glyph #ffffff; hover fill `color-mix(#a9bd55 65%)` olive glyph **#0a0a0c**; disabled #ffffff 10% glyph #ffffff 30% · spacing surface padding 14px 16px 8px; textarea padding 6px 4px 12px min-height 58px; provider/workspace pad 4px 10px min-h 28px; model pad 4px 12px min-h 28px; image-picker 30×30
**Controls:** Order via `order`: attach(0) permission(1) ensemble-mode(2) provider(3) workspace(4) model(5, margin-left:auto). Left cluster flex gap 6px flex:1. Provider & workspace identical outlined capsules (transparent, 1px white/14% border, white/78% text); hover bg→white/6% (workspace border→olive 40%). Model brighter capsule (1px white/18% border, white/88% text, xs+1) hover bg white/6% border white/28%. Permission plain text (svg display:none). Image/attach 30×30 transparent circle. Ensemble-mode 999px segmented, is-active fills olive `color-mix(#a9bd55 70%)` #0a0a0c text weight 600. NOTE earlier rule (07:2416-2433) tints provider/workspace/ensemble-label chips with an olive WASH (bg `color-mix(#84a33b 22%)`, border `color-mix(#bbcf66 34%)`, text `color-mix(#d4e79a 96%)`) — both rule-sets live; 2624+ wins on the bare chip, 2416 colors label/picker variants.
**Send glyph:** arrow-up
**Typography:** Sans. Textarea `var(--font-size-md)`. Placeholder NOT italic (white/36%). provider/workspace/ensemble `--font-size-xs`; model & permission `calc(--font-size-xs + 1px)`; weights 500, active ensemble 600. Model caret text glyph '▾' 9px opacity 0.6 margin-left 4px. No tabular-nums.
**Row policy:** Shared above-bar-stack; NO tucked-tab/merged-instrument variant. Rows styled as sibling cards matching surface (18px, fill `color-mix(#0a0a0c 96%)`, 1px white/8%, `0 4px 16px rgba(0,0,0,0.5)`, ::before suppressed). Aura matrix places kimi in "stacked, aura-suppressed" group: stack box-shadow forced none, stack ::after display:none/content:none — keeps rim/edge/sidebar aura, drops stack glow + provider ::after wash. Obsidian/dark-ensemble re-assert solid fills + null ::before/::after. Suppression rule (07:2553-2556) hides top-toggles/chips/non-whitelisted left children.
**Effects:** Surface ::before SUPPRESSED (kills blue edge-gradient). Above-bar ::before SUPPRESSED. Model ::after VISIBLE '▾' 9px opacity 0.6 (real SVG hidden). Focus ring (layered box-shadow) on :focus-within: `0 4px 16px rgba(0,0,0,0.55), 0 0 0 3px color-mix(#a9bd55 10%)` + border `color-mix(#a9bd55 50%)`; OFF at rest. Agent-aura stack ::after SUPPRESSED (05:3044-3052) + provider-kimi transcript ::after wash zeroed. Obsidian/dark-ensemble force all row ::before AND ::after none. No paper/perforation/caret/two-surface split.
**Theme behavior:** Theme-immune at core (hardcodes #0a0a0c surface, #ffffff/8% border, #ffffff/36% placeholder, #a9bd55 accent regardless of theme; no --surface-* in dark). Flips: (1) Light family + alabaster re-key surface to surface-2/unified-soft, focus darker olive #84a33b (42%/12%), chips olive-on-cream (#84a33b 10-12% bg / #34410f text); (2) Obsidian solid dark #202124/#1f2023, white/12% borders, inverted text rgba(246,246,246,0.84), active ensemble olive #a9bd55 70% on #0a0a0c; (3) Alabaster cream remap olive chips + run-btn bg `color-mix(#84a33b 18%, #fff)` glyph #34410f. Signature olive across family (#a9bd55 dark, #84a33b/#bbcf66/#d4e79a light).
**A11y fallbacks:** No kimi-specific rules (inherits global). reduce-transparency → no-op (opaque, no backdrop-filter; fill swap inert since 07 wins over 03). reduce-motion → nothing in the composer animates (focus shadow is static). forced-colors → global block hands inputs/inner-module to system colors; SwiftUI should drop olive accent + custom fills for system label/fill in high-contrast.
**Skeptic:** accurate=true; corrections applied = none. Non-material nits only (stale "~main.css:6566" comment → actual base ::before at 03:2767; reduce-transparency fill swap inert; stop-btn not explicitly 32px in kimi). Send glyph arrow-up matches global facts. Confidence: high.
**Evidence:** `07-composer-shells.css:2553-2556` (suppression), `:2564-2572` (above-bar), `:2574-2593` (surface 18px + focus olive), `:2614-2622` (order), `:2658-2668` (permission plain text), `:2671-2693` (model + ▾), `:2714-2738` (ensemble active olive), `:2740-2759` (send 32px), `:2416-2433` (olive wash variant); `05-polish-fx-layouts.css:2644-2683` (aura + transcript wash zeroed), `:3011-3052` (aura-suppressed group); `08-theme-picker-overrides.css:3974-4009, 4397-4400`; `10-provider-shell-overrides.css:753-938` (obsidian), `:3171-3368` (light/alabaster); `App.tsx:20681-20682` (ArrowUpSendIcon).

### cursor — Cursor (visual-only, flat gray, theme-immune)
**Essence:** Deliberately chroma-free flat neutral-GRAY shell imitating Cursor's input (a rounded capsule + satellite row) with all color/glass/gradient stripped. Gemini-style merged frosted-gray above-bar stack with git/Create-PR rows floated ABOVE as detached gray pills; flat-gray textarea capsule below with [+]/model/send anchor-lifted in; permission/ensemble/provider/workspace as a transparent satellite row.
**Material:** Flat solid gray, no glass/gradient/chroma. Tokens on `<html>` root (dark default, 10:2067-2087): `--cursor-shell-bg #1f1f1f`, `--cursor-shell-surface #2a2a2a`, `--cursor-shell-fg #d8d8d8`, `--cursor-shell-muted rgba(216,216,216,0.6)`, `--cursor-shell-faint rgba(216,216,216,0.38)`, `--cursor-shell-border rgba(216,216,216,0.16)`, `--cursor-shell-border-strong rgba(216,216,216,0.42)`, `--cursor-shell-hover rgba(216,216,216,0.09)`, `--cursor-shell-fill rgba(216,216,216,0.1)`, `--cursor-shell-rim rgba(216,216,216,0.22)`, `--cursor-shell-rim-strong rgba(216,216,216,0.42)`, `--cursor-shell-stack-header #101010`. Light/system flip: bg #ededed, surface #f4f4f4, fg #2e2e2e, muted rgba(46,46,46,0.62), faint rgba(46,46,46,0.4), border rgba(46,46,46,0.16), border-strong rgba(46,46,46,0.4), hover rgba(46,46,46,0.06), fill rgba(46,46,46,0.08), rim rgba(46,46,46,0.14), rim-strong rgba(46,46,46,0.3), stack-header transparent. CAPSULE = SOLID `var(--cursor-shell-surface)` no blur. Above-bar stack + floating rows DO use `backdrop-filter: blur(18px) saturate(140%)` over `color-mix(var(--cursor-shell-surface) 72%, transparent)` (the only translucency); `.composer-surface` transparent backdrop-filter:none. Portaled menus solid surface backdrop-filter:none.
**Palette:** surfaceFill capsule **#2a2a2a** D / **#f4f4f4** L; stack + floating rows `color-mix(surface 72%, transparent)`; surface transparent; app pane bg #1f1f1f / #ededed · innerModuleFill **n/a** (capsule is the filled module) · border hairline `--cursor-shell-border` rgba(216,216,216,0.16)/rgba(46,46,46,0.16); capsule + stack/rows `--cursor-shell-border-strong` rgba(216,216,216,0.42)/rgba(46,46,46,0.4) · focusAccent **no chroma** — focus = capsule border-color → `--cursor-shell-rim-strong` + inset 0 1px 0 rim-strong; --accent remapped to fg in portaled menus · textPrimary `--cursor-shell-fg` #d8d8d8 / #2e2e2e · placeholder `--cursor-shell-faint` rgba(216,216,216,0.38)/rgba(46,46,46,0.4) · rimOrGlow flat top-edge lip `inset 0 1px 0 --cursor-shell-rim` on capsule (rgba(216,216,216,0.22)/rgba(46,46,46,0.14)) + on stack/rows alongside `0 4px 28px rgba(0,0,0,0.28)`. **At rest no outer glow — BUT during agent activity the merged stack DOES paint a colored aura [corrected, see Effects]**
**Geometry:** surfaceRadius merged stack `--cursor-shell-stack-radius` **14px**; surface radius 0 · controlShape mixed: workspace/provider 999px outline pills; ensemble-mode 999px segmented; + button 999px 30px circle; model bare inline label radius 8px + drawn caret; permission plain text · innerRadius capsule `--cursor-shell-capsule-radius` **26px**; above-bar pills 999px · sendButton **32px circle (999px)**, fill `--cursor-shell-fg` #d8d8d8/#2e2e2e, glyph color `--cursor-shell-bg` #1f1f1f/#ededed, border 0, box-shadow none; hover fg fill opacity 0.85; stop/steer 32px fill `--cursor-shell-fill` + 1px border; disabled 32px fill + faint glyph · spacing capsule min-height 50px padding 3px 0; single-h `calc(1.55*--font-size-md+30px)`; textarea padding `13px 224px 5px 46px`; lift: attach +16px/left 10px, model +17px/right 50px max-width 172px, send +16px/right 12px; surface flex column gap 6px; stack gap 0 margin-bottom 6px
**Controls:** Single merged feel split across two zones. LIFTED into capsule row (abs, anchored `--composer-capsule` bottom): [+] (30px gray-fill circle, left 10px), model label (bare text + CSS ::after caret, right 50px max 172px ellipsis), send-cluster (right 12px). DEMOTED to transparent satellite row (footer display:contents): permission(1, plain text svg hidden), ensemble-mode(2, 999px segmented — active inverse fg-on-bg weight 600), provider(3, 999px outline pill), workspace(4, 999px outline pill 12px icon). Hover = transient neutral `--cursor-shell-hover`. Telemetry strip below, transparent.
**Send glyph:** arrow-up
**Typography:** System sans. Textarea `var(--font-size-md)` line-height 1.55; permission + model `calc(--font-size-xs + 1px)` weight 500; workspace/provider/ensemble `--font-size-xs`; ensemble active weight 600. Sidebar headers uppercase 10px letter-spacing 0.06em faint. Welcome hero h1 clamp(28px,4vw,44px) weight 400 (strong 600) letter-spacing -0.01em. No mono/serif/italic/tabular-nums.
**Row policy:** Above-rows SPLIT by JSX — `aboveRowsFloatAboveStack` true for cursor (App.tsx:18185-18186): git/Create-PR primary + secondary-workspace rows tagged `.composer-above-bar--cursor-lead`, render as DETACHED satellite pills ABOVE the merged stack (codex shares). Merged stack wraps Roster/Ensemble/Steer-Queue/external-path. Via `:has()`: `.composer-area:has(.composer-above-bar-stack)` → frosted-gray frame 14px segmentation dividers; `:has(.composer-above-bar--cursor-lead) .composer-above-bar-stack:empty` → fully hidden. First/last children rounded outer corners; first child darker stack-header fill (#101010 dark); consecutive separated by border-top hairlines. Create-PR reflows to 3 ordered pills changes|git|action (orders 30/10/80, git flex 1).
**Effects:** `.composer-surface::before` content:none. All above-row ::before/::after killed. Model-picker caret ::after (10:2493-2504): 6px box, border-right + border-bottom 1.5px `--cursor-shell-muted`, `translateY(-2px) rotate(45deg)` (hover → fg) — the ONLY decorative pseudo-element ADDED. `[data-interface-style="cursor"] .app-transcript::after` display:none. Merged stack inner segmentation (transparent rows, border-top hairlines, first-child stack-header fill). **AGENT-AURA: NOT absent [corrected — spec was materially WRONG].** `composerAboveBarStackAuraClass = composerAgentAuraClass` for EVERY style except 'default' (App.tsx:15538-15542, applied 18387), so cursor's merged stack receives `fx-agent-aura fx-status-*`. The GENERIC selectors `:is(.composer-surface,.composer-above-bar-stack).fx-agent-aura` (05:2926, rim `0 0 0 1px rgb(aura/0.28), 0 18px 58px /0.18`) and running `::after` pulse (05:2934-2950, `0 0 0 1px rgb(aura/0.5) + 0 22px 68px /0.24`, 2.8s) FIRE on cursor's filled stack; comment 05:3009-3010 names Cursor as keeping the stack treatment. Glow keyed to fx-provider-{provider|ensemble} (independent of gray tokens); approval warn-rgb, failed danger-rgb. Glow killed only on `.composer-surface` (10:2279), NOT the stack.
**Theme behavior:** Theme-immune BOTH directions (like Obsidian/Alabaster) but locks to GRAY and flips gray-on-dark vs dark-on-gray, never chroma — **except the activity aura, which IS chroma [corrected]**. Dark default on dark themes; `:is(light,mist,sage,alabaster)` + `@media(prefers-color-scheme:light)[data-theme=system]` flip to light-gray. Every prop reads `--cursor-shell-*` (which flip). Appended at file foot; color props !important to beat native-glass light-frost. Never the mustard provider color (decoupled).
**A11y fallbacks:** No cursor-specific reduce-motion/high-contrast. reduce-transparency → only targeted degradation is `[data-appearance="native_glass"][data-reduce-transparency="false"]` → backdrop-filter:none on floating rows (10:2163-2174) + merged stack (10:2189-2196); capsule/surface never blur. **reduce-motion → cursor stack aura running-pulse IS suppressed by 05:2955-2959** `:root[data-reduce-motion="true"] :is(.composer-surface,.composer-above-bar-stack).fx-agent-aura.fx-status-running::after {animation:none;opacity:0}` [corrected — targeted rule, not just global]. No forced-colors handling.
**Skeptic:** accurate=false; corrections applied = (1) **cursor's merged stack DOES show a colored agent-aura glow + pulse during activity** (05:2926, 2934-2950 via App.tsx:15538-15542 + 18387) — "never chroma / no glow" is true ONLY at rest; (2) "never any chroma" overstated — activity glow is provider/ensemble-tinted; (3) reduce-motion has a dedicated rule suppressing the stack aura pulse (05:2955-2959). SwiftUI must render an activity glow on the merged stack. Send glyph arrow-up matches global facts. Confidence: high.
**Evidence:** `10-provider-shell-overrides.css:2067-2120` (token sets dark+light), `:2148-2233` (merged stack + segmentation), `:2236-2268` (cursor-lead floats + above-row ::before kill), `:2275-2341` (transparent surface + capsule), `:2374-2399` (anchor-lift), `:2443-2504` (permission/model + caret), `:2547-2579` (send 32px), `:2163-2174,2189-2196` (native_glass reduce-transparency); `App.tsx:15538-15542, 18185-18186, 18387, 20677-20682`; `05-polish-fx-layouts.css:2926, 2934-2950, 2955-2959, 3009-3010` (generic aura FIRES on cursor stack + reduce-motion).

### grok — Grok (visual-only, monochrome, theme-immune)
**Essence:** Strictly monochrome black/white shell — zero chroma, zero glass, zero gradients. Copies Gemini's control ordering but renders every picker as a bare floating label (ensemble-mode is the lone outline capsule). Flat 24px-radius card with a signature bright top-edge inner rim. Above-rows use a Codex-style "tucked tab".
**Material:** Solid + flat, NO glass anywhere. `backdrop-filter:none` + `-webkit-backdrop-filter:none` forced (!important) on the surface, above-row stack, diff-action menu, **app sidebar**, and all portaled popovers — **NOT the app transcript** [corrected — transcript is only recolored: background + ::after kill, no backdrop-filter declaration]. No translucent fills on structural chrome; all provider/theme ::before gradients killed (content:none + background:none). Surface fill dark **#111111** / light #f7f7f7. Page/above-row bg dark **#000000** / light #ffffff. Only translucency is white/black alpha tokens.
**Palette:** surfaceFill **#111111** D / **#f7f7f7** L · innerModuleFill **n/a** (`.composer-chips` display:none; transparent textarea) · border `--grok-border-strong rgba(255,255,255,.5)` D / `rgba(0,0,0,.42)` L on surface+rows; hairline `--grok-border rgba(255,255,255,.16)` / `rgba(0,0,0,.16)` · focusAccent `--grok-rim-strong rgba(255,255,255,1)` / #ffffff — on focus-within surface border AND top rim go full-opacity white · textPrimary `--grok-fg #ffffff` D / `#000000` L · placeholder `--grok-faint rgba(255,255,255,.34)` / `rgba(0,0,0,.34)` · rimOrGlow **signature top-edge inner rim** `inset 0 1px 0 --grok-rim` = `rgba(255,255,255,.78)` D / `rgba(255,255,255,.95)` L; NO outer glow, NO colored shadow; agent-aura suppressed entirely
**Geometry:** surfaceRadius **24px**; above-row tucked-tab `calc(--radius-lg * 1.6)` = 14×1.6 = **22.4px** top corners only, bottom radius 0, bottom border 0, margin-bottom -10px · controlShape NOT pills — bare floating labels (transparent, border 0, radius 8px hit-area, padding 4px 6px) for workspace/model/provider; permission plain text padding 0 6px; EXCEPTION ensemble-mode capsule (999px, 1px --grok-border, padding 1px); attach 30px transparent circle · sendButton **32×32px circle (999px)**, run fill `--grok-fg` (white D/black L) glyph `--grok-bg` (inverse) border 0 box-shadow none; stop/steer 32px fill `--grok-fill` fg glyph 1px border; disabled 32px fill `--grok-fill` faint glyph · spacing surface padding 14px 18px 8px; control row gap 6px left cluster flex:1; model margin-left:auto; above-row stack width `min(100%−80px, 900px)` centered padding-bottom 14px, solo also padding-top 6px; textarea padding 6px 6px 10px min-height 48px
**Controls:** Order attach(0) permission(1) ensemble-mode(2) provider(3) workspace(4) model(5, margin-left:auto). Hidden: top-toggles, chips, any non-{attach,ensemble-mode,permission,model,provider,workspace,guest} left child. Attach 30px transparent ghost circle. Permission plain-text (svg display:none), --grok-muted, xs+1px weight 500. Provider/workspace bare muted labels, hover → --grok-hover bg + fg; workspace icon 12px opacity .7. Model bare label (primary full fg, reasoning suffix muted), caret CSS ::after. Ensemble-mode outline capsule; inner button muted (padding 3px 10px), .is-active inverts bg-on-fg (color --grok-bg, bg --grok-fg, weight 600). Final sign-off rule (10:1862-1887) strips grok's ensemble-mode picker-label chrome.
**Send glyph:** arrow-up
**Typography:** System sans (NOT mono despite the look). Textarea `var(--font-size-md)`; permission/model `calc(--font-size-xs + 1px)` weight 500; provider/workspace/ensemble `--font-size-xs`. Placeholder NOT italic (--grok-faint). Sidebar headers uppercase 10px ls .06em. Welcome hero clamp(22px,2.45vw,30px) weight 430 (strong 720). No tabular-nums.
**Row policy:** KEY `:has()` solo-vs-merged switch. SOLO (`:not(:has(> :nth-child(2)))`): single above-row IS the tucked tab — width `min(100%−80px,900px)` centered, top-radius 22.4px, bottom square, bottom border 0, margin-bottom −10px, pad 6/14, bg --grok-bg, 1px --grok-border-strong, inset top rim. MULTI (`:has(> :nth-child(2))`): STACK becomes merged frame (same width/radius/border/rim/−10px, overflow hidden, gap 0); each child flattened (border 0, radius 0, transparent, no shadow, margin 0, full width) separated by 1px --grok-border top-border (except compact roster picker). Merged rows nowrap flex gap 6px; files-cluster margin-left 6px; diff-action-menu margin-left:auto. Agent-aura ::after suppressed for grok in ALL states (grouped with codex; box-shadow:none + content:none).
**Effects:** Top-edge rim highlight (PRIMARY): `inset 0 1px 0 --grok-rim` on surface AND above-row tab/stack (rgba 255,255,255,.78 D / .95 L); brightens to --grok-rim-strong (full white) on focus. Model caret pure-CSS ::after (6px box, border-right+bottom 1.5px --grok-muted, rotate(45deg) translateY(-2px), hover → fg); real SVG chevrons hidden. Gradient-overlay KILL on every provider/theme ::before. Merged-row hairline dividers (real border). Agent-aura glow SUPPRESSED (codex/grok exclusion arm). Ensemble-mode active inverse fill (solid swap).
**Theme behavior:** THEME-IMMUNE and bidirectional — locks monochrome regardless of provider/agent color. Tokens on `<html>` root, cascade to composer + app chrome + portaled popovers (.shell-grok). DARK = white-on-black (bg #000, surface #111, fg #fff, rim .78). LIGHT (light/mist/sage/alabaster) = black-on-white (bg #fff, surface #f7f7f7, fg #000, border-strong rgba(0,0,0,.42), rim .95). system via `@media(prefers-color-scheme:light)`. !important on color props. Send circle inverts per theme (uses --grok-fg/--grok-bg).
**A11y fallbacks:** reduce-transparency → no grok rule needed (zero backdrop-filter); global rule strips backdrop-filter + sets --composer-bg-solid which grok overrides to --grok-surface → unchanged. reduce-motion → no shell animations (aura already suppressed); global arm sets the hidden aura ::after animation:none — no visible change. high-contrast/forced-colors → only the global block (body→Canvas/CanvasText, .composer-inner-module forced-color-adjust:auto); no grok-specific rule; SwiftUI in Increase-Contrast should raise --grok-border-strong toward full opacity and keep the rim.
**Skeptic:** accurate=false; corrections applied = backdrop-filter:none is forced on the app SIDEBAR (10:1922-1923) but NOT the transcript (10:1949-1955 only recolors + kills ::after) — material scope correction. Send glyph arrow-up matches global facts. Confidence: high — every hex/radius/the `:has(>:nth-child(2))` switch/control order/send/caret/gradient kills/aura suppression/font tokens/theme-immunity verified; only transcript backdrop-filter overstated.
**Evidence:** `10-provider-shell-overrides.css:1371-1415` (--grok-* tokens dark/light/system), `:1418-1422` (hidden controls), `:1427-1520` (KEY solo-vs-merged tucked-tab), `:1531-1541` (gradient kills), `:1546-1572` (surface 24px + rim + transparent textarea), `:1584-1589` (order), `:1667-1678` (caret ::after), `:1696-1720` (ensemble capsule + inverse), `:1724-1755` (send 32px inverse), `:1922-1923` (sidebar backdrop none), `:1949-1955` (transcript recolor only); `05-polish-fx-layouts.css:3024-3284` (aura suppression); `App.tsx:20675-20685` (ArrowUpSendIcon); `theme.css:209` (--radius-lg 14px → 22.4px).

### modular — Modular
**Essence:** Deletes the composer container entirely — the surface goes fully transparent/borderless/shadowless and every element re-materializes as its own free-standing pill casting a soft drop shadow over the page. A constellation of independent micro-cards; no grouping wrapper.
**Material:** Container transparent. Textarea becomes a distinct 18px-radius card; each above-bar chip + inline picker becomes a 999px capsule; send is an accent-filled 999px capsule. `backdrop-filter` / `-webkit-backdrop-filter` stripped (none !important) on surface (08:6669-6670), above-bar (08:6909-6910), and ensemble rows (09:1765-1766). Theme-derived from `--composer-bg-solid` / `--panel-border` / `--accent`. DARK/default (08:4831-4892) = composer-bg-solid 88% fills + rgba(0,0,0,.12-.22) shadows; LIGHT (light/mist/sage, 08:4894-4906) swaps pill fill → `--unified-soft-surface-bg`, border → `--unified-soft-surface-border`, shadow → `0 2px 8px rgba(0,0,0,.05)`.
**Palette:** surfaceFill **transparent** (container forced transparent !important) · innerModuleFill **n/a** (textarea IS the inner module; fill composer-bg-solid 88%) · border none on container; each pill `1px solid color-mix(var(--panel-border) 82%, transparent)`; light `var(--unified-soft-surface-border)` (= surface-border) · focusAccent textarea focus ring `0 0 0 3px color-mix(var(--accent) 14%, transparent)` + border `color-mix(var(--accent) 48%, var(--panel-border))`; send capsule var(--accent) · textPrimary var(--text-primary) (explicit only in light; dark inherits base) · placeholder inherited from base (no modular override) · rimOrGlow per-pill drop shadow: textarea `0 4px 18px rgba(0,0,0,.18)`; above-bar pills + pickers `0 2px 10px rgba(0,0,0,.12)`; send `0 4px 14px color-mix(var(--accent) 28%, transparent)`. **Light variant: textarea shadow `0 2px 8px rgba(0,0,0,0.05)` (08:4904); light ensemble above-row + compact roster picker `0 4px 14px rgba(0,0,0,0.08)` (09:1221) + bg `color-mix(--surface-2 88%)` + border `color-mix(--surface-border 82%)`. The `0 4px 18px rgba(0,0,0,0.18)` value is DARK-only (09:1764)** [corrected — spec wrongly claimed light keeps 0 4px 18px /.18]
**Geometry:** surfaceRadius **0/none** (transparent, shapeless; padding 6px 0 4px); visual radius lives on children · controlShape **capsule(999px)** every chip/picker (picker-label/-command/image-picker-btn/control-button/schedule-label/runtime-profile); container flat transparent gap:6px; above-bar gap:8px padding 0; each above-bar child padding 4px 10px · innerRadius **textarea 18px** padding 14px 16px + padding-top:44px (clears top-right scheduler/runtime pickers); ensemble above-row + compact roster mirror textarea (18px, composer-bg-solid 88%, panel-border 82%, 0 4px 18px, blur stripped) · sendButton **40×40px (base) r999**, fill `color-mix(var(--accent) 86%, transparent)`, border `1px color-mix(var(--accent) 56%, var(--panel-border))`, shadow `0 4px 14px color-mix(var(--accent) 28%)`; glyph = RunSymbolIcon 28×28 color `var(--success)` green (inherited — modular sets only bg/border/shadow/radius); stop-btn shares accent capsule but glyph `var(--danger)` · spacing pure flex gaps (above-bar 8px, footer 6px); default order unchanged; files/diff clusters pushed right (margin-left:auto/order:80, shared new-shell group)
**Controls:** Inline-pickers + footer de-chromed (transparent, border 0, box-shadow none, gap 6px) so each picker puffs into a 999px capsule (composer-bg-solid 88% fill + panel-border 82% hairline + `0 2px 10px rgba(0,0,0,.12)`). No capsule/lift anchoring. Send run/stop = accent-filled capsules. Above-bar container flat, each direct child a 999px pill padded 4px 10px. No ::after carets. Telemetry untouched (base styling).
**Send glyph:** run-triangle
**Typography:** Inherits app default sans (no override — unlike terminal mono). No size deltas for modular; placeholder/italic/tabular-nums from base.
**Row policy:** NON-MERGED / inside-stack-dissolved family. Stack-level aura would paint a sharp rect behind the gap-only stack → suppressed: stack box-shadow none !important + stack ::after killed. Aura drops to EACH present row: every `> child` gets position:relative (05:3087) + ::after rim+glow with border-radius:inherit. Per-row presence: rows glow only if they exist. Layout :has() also drives files-cluster/diff-action margin + secondary-row trailing margin-left:auto.
**Effects:** Container ::before KILLED (display:none + re-killed display:none + background:none !important with ::after). Above-bar ::before background:none + ::before/::after display:none/background:none/box-shadow:none !important. **Per-pill drop shadows = signature effect** (real box-shadows). Textarea focus ring (border accent-48% + `0 4px 18px rgba(0,0,0,.22)` + `0 0 0 3px accent-14%`). Per-row agent-aura ::after (only when stack `.fx-agent-aura`): idle `0 0 0 1px aura-rgb/.28 + 0 18px 58px /.18`; running `/.5 + 0 22px 68px /.24` + pulse 2.8s; approval warn no anim; failed danger no anim. backdrop-filter stripped so transparent reads see-through.
**Theme behavior:** Theme-derived. DARK: composer-bg-solid 88% + rgba(0,0,0,.12-.22) shadows, ink var(--text-primary). LIGHT (light/mist/sage): pill fill → unified-soft-surface-bg/border, shadow → `0 2px 8px rgba(0,0,0,.05)`, color → text-primary; above-bar branch chip stripped to transparent text (10:3370-3378); above-bar pill descendants soft-surface, -files/-stats text labels EXCLUDED (no white strip behind counts). Light ensemble override at 09:1215-1223.
**A11y fallbacks:** reduce-motion → running per-row aura animation:none + opacity:0 (05:3247-3284); idle states already static. reduce-transparency → NO modular rule (container already transparent, pills near-opaque 88%); SwiftUI should raise pill fills to 100% opaque. No forced-colors block; SwiftUI Increase-Contrast should thicken hairlines + drop drop-shadows for solid strokes.
**Skeptic:** accurate=false; corrections applied = light-variant shadows corrected — textarea softens to `0 2px 8px rgba(0,0,0,0.05)` (08:4904); light ensemble row/compact roster get own override `0 4px 14px rgba(0,0,0,0.08)` + surface-2/surface-border/surface-text (09:1215-1223); `0 4px 18px /.18` is DARK-only (09:1764). Send glyph run-triangle matches global facts. Confidence: high.
**Evidence:** `08-theme-picker-overrides.css:4817-4906` (surface transparent + textarea pill + above-bar/picker capsules + send accent + light variant), `:4212-4219` (var defs), `:6631-6675` (padding-top 44 + bulletproof transparent), `:6905-6948` (above-bar transparent + light descendants), `:8899-8976` (files/diff layout); `09-ensemble-work-session.css:1215-1223` (light ensemble override), `:1759-1767` (dark ensemble mirror); `05-polish-fx-layouts.css:3011-3284` (stack aura suppressed + per-row rim + reduce-motion); `03-composer-welcome-activity.css:6324-6398` (base 40×40 r999 + run glyph success/danger); `App.tsx:20675-20685` (else → RunSymbolIcon).

### terminal — Terminal
**Essence:** REPL/terminal emulator shell — near-black green-tinted surface, sharp 2px corners, monospace everything, a bright green `>` prompt caret top-left of the input, picker chips wrapped in literal `[ ]` brackets, small square green send button with an outline run-triangle. Forces its dark green look even under light app themes.
**Material:** Solid, opaque, non-glass (NO backdrop-filter). Surface fill `color-mix(in srgb, #0a0e0a 92%, #00ff88 8%)` (08:4916) — same light/dark, light variant re-states with !important + `color-scheme:dark`. Above-bar/ensemble fill `color-mix(#0a0e0a 78%, transparent)`. Already opaque → reduce-transparency no-op.
**Palette:** surfaceFill `color-mix(#0a0e0a 92%, #00ff88 8%)` (forced !important in light) · innerModuleFill **n/a** (inner-module/chips/footer/inline-pickers transparent; textarea on surface) · border `1px color-mix(#00ff88 24%, transparent)` (focus `color-mix(#00ff88 52%)`; above-bar `color-mix(#00ff88 18%)`) · focusAccent **#00ff88** — focus ring `0 0 0 2px color-mix(#00ff88 18%, transparent)`; prompt caret `color-mix(#00ff88 86%, #ffffff)` · textPrimary `color-mix(#ccffe4 92%, #ffffff)` pale mint (chips `color-mix(#ccffe4 84%)`) · placeholder `color-mix(#ccffe4 36%, transparent)` font-style:normal · rimOrGlow `inset 0 1px 0 color-mix(#00ff88 8%, transparent)` green top hairline + `0 6px 18px rgba(0,0,0,0.4)` drop; focus bumps inset to 12% + 2px green ring; textSecondary timecode `color-mix(#ccffe4 72%/68%)` (running #00ff88); sendButton fill `color-mix(#00ff88 24%, #0a0e0a)` border `color-mix(#00ff88 56%)` glyph #ccffe4
**Geometry:** surfaceRadius **2px** (above-bar 2px; ensemble-above-row 2px) · controlShape **rect 2px** every chip/button/image-picker/schedule/runtime/ensemble-wrapper; inner ensemble buttons radius:0 · sendButton **square ~2px-radius, 40×40px** (base, only bg/border/color/radius overridden to terminal green; base radius 999px overridden to 2px); glyph outline run-triangle (RunSymbolIcon `M6.1 4v8l4.8-4-4.8-4z` fill:none stroke:currentColor #ccffe4) 28×28, effective stroke-width 2.2 · innerRadius n/a · spacing surface padding 12px 14px; textarea padding 4px 4px 6px 18px (18px clears `>`) but padding-top:44px batch slides caret to top:50px/left:14px; textarea min-height 56px; chips padding 2px 6px; above-bar padding 4px 10px
**Controls:** **No explicit order block — falls back to raw DOM order [corrected — spec wrongly cited 07:94-99, which is `[data-composer-style="default"]`-scoped; no terminal control-order rule exists].** All pickers/footer transparent. Each chip transparent rect, faint green hairline `color-mix(#00ff88 14%)`, 2px radius, mono, `calc(--font-size-xs - 1px)`, text `color-mix(#ccffe4 84%)`; hover border 32% + 6% green wash. Picker LABELS get literal brackets: `[` via `.composer-picker-label > span:first-child::before` (margin-right:2px opacity 0.6) and `]` via `.composer-picker-label::after` (margin-left:2px opacity 0.6); runtime uses span::before "[" / select::after "]". Ensemble-mode 1px-padded green-bordered rect holding flush radius:0 buttons; active `color-mix(#00ff88 22%)` fill #ccffe4 text weight 600. Send/stop square green. Telemetry text `color-mix(#ccffe4 68%)`, timecode mono, running timecode solid #00ff88. (Above-bar margin/order layout group at 08:8899-8976 DOES include terminal.)
**Send glyph:** run-triangle
**Typography:** Monospace everywhere via `var(--font-mono)` ("SF Mono","JetBrains Mono","Cascadia Code",Menlo,Monaco,"Courier New",ui-monospace,monospace; theme.css:235) on surface/textarea/above-bar/pickers/footer/ensemble/timecode. **Composer uses the GLOBAL `*1` scale, NOT the sidebar `*1.17` [corrected — 1.17 is scoped to `.app-sidebar`; composer renders in the transcript and inherits theme.css:236-237 `*1`].** At 16px root: `--font-size-xs` ≈ 0.936rem ≈ **14.98px**, `--font-size-sm` ≈ 1.066rem ≈ **17.06px**; textarea `calc(--font-size-sm - 1px)` ≈ **16px**; chips/above-bar/ensemble/prompt-caret `calc(--font-size-xs - 1px)` ≈ **14px**. Caret `>` font-weight 700; active ensemble button weight 600. Placeholder font-style:normal (NOT italic). No tabular-nums. Textarea text-caret (caret-color) NOT overridden — inherits var(--text-primary).
**Row policy:** No terminal-specific `:has()` for shell chrome (above-bar/ensemble styled unconditionally). Only `:has()` from the shared aura matrix: terminal in the standard `:is(...)` group — when stack `.fx-agent-aura`, stack glow suppressed (box-shadow none, ::after content none) and EACH child row gets position:relative + ::after rim-glow `inset:0; border-radius:inherit; box-shadow 0 0 0 1px rgb(.../.28), 0 18px 58px /.18`; running `/.5 + 0 22px 68px /.24` + pulse 2.8s; approval warn `/.5` no anim; failed danger `/.42` no anim. NOT in grok/codex solo-row branch.
**Effects:** `.composer-surface::before` PROMPT CARET — content:">", absolute top:14px→50px / left:14px, width:12px height:18px, mono weight:700, color `color-mix(#00ff88 86%, #ffffff)`, z-index:2 (signature glyph; ON whenever shell active; light color re-stated !important). (Earlier dead override display:none'd ::before, then RE-DEFINED with ">" — later rule wins.) Bracket `[` (`.composer-picker-label > span:first-child::before` opacity 0.6) and `]` (`.composer-picker-label::after` opacity 0.6). Above-bar ::before background:none. Focus ring `0 0 0 2px color-mix(#00ff88 18%)`. Inset green top hairline (scanline). Per-row agent-aura rim. Send/stop square green, base hover translateY(-1px)/active scale(0.98) + 28px outline triangle.
**Theme behavior:** Theme-IMMUNE by design — locks dark green, refuses light. In light family re-states dark fill + border !important + `color-scheme:dark`. Out-specifies provider light-surface rules (hence !important). Cascade of (0,3,0) light overrides keeps every sub-element green: textarea/placeholder/caret, above-bar, runtime/schedule pickers + native select/input, ensemble-mode toggle, timecode (light running `color-mix(#00ff88 82%, #ffffff)`). Dark themes use base unchanged → identical green-on-near-black everywhere.
**A11y fallbacks:** reduce-motion → no intrinsic animation; aura running rim-pulse killed (animation:none + opacity:0); base send hover/active transforms not gated. reduce-transparency → no-op (opaque, no backdrop-filter). No terminal-specific forced-colors; SwiftUI high-contrast should keep 2px borders, raise green border alpha (14-24% most at-risk), ensure caret/chip text contrast.
**Skeptic:** accurate=false; corrections applied = (1) **font-size uses global `*1` scale not sidebar `*1.17`** → xs ≈14.98px, sm ≈17.06px, textarea ≈16px, chips/caret ≈14px (theme.css:236-237, not 01-sidebar.css); these bad px also propagate to spacingNotes + caret-size; (2) **no terminal control-order block — DOM order, not the default-shell order** (07:94-99 is default-scoped). Send glyph run-triangle matches global facts. Confidence: high.
**Evidence:** `08-theme-picker-overrides.css:4915-4949` (surface + textarea mono), `:4972-4988` (::before ">" caret), `:6631-6642` (padding-top 44 + caret top:50), `:4991-5036` (above-bar + chips), `:5043-5070` (ensemble toggle), `:5087-5098` (brackets), `:5100-5107` (send square green), `:5113-5226` (forced-dark light overrides); `09-ensemble-work-session.css:1876-1897`; `05-polish-fx-layouts.css:3010-3284` (shared aura); `03-composer-welcome-activity.css:6324-6373` (base btn + 28px glyph); `theme.css:235, 236-237` (font-mono + global scale); `AppChromeSymbols.tsx:398-413` (RunSymbolIcon); `App.tsx:20675-20685` (else → RunSymbolIcon).

### stub — Ticket stub
**Essence:** Cinema/cloakroom ticket on cream paper — surface AND above-bar are the same opaque cream card over a faint diagonal "paper grain", warm tan border, 14px radius, with a dashed gold "perforation" line separating ticket head from body. Serif throughout, italic placeholder, gold send button.
**Material:** Opaque PAPER, no glass. Two stacked layers: top = `repeating-linear-gradient(135deg, rgba(255,248,230,0.04) 0 2px, transparent 2px 6px)` (grain), bottom = `color-mix(#fcf6e6 14%, var(--composer-bg-solid))` (composer-bg-solid defaults var(--surface-1)). No backdrop-filter (ensemble row sets backdrop-filter:none). Light/mist/sage: grain `rgba(120,90,40,0.03)` over flat **#fbf3df**. Surface box-shadow `inset 0 1px 0 color-mix(#ffffff 6%, transparent), 0 8px 22px rgba(0,0,0,0.18)`; focus `inset 0 1px 0 #ffffff@8%, 0 10px 26px rgba(0,0,0,0.22)`. Above-bar shadow lighter `0 4px 14px rgba(0,0,0,0.14)`.
**Palette:** surfaceFill `color-mix(#fcf6e6 14%, var(--composer-bg-solid))` over 135deg `rgba(255,248,230,0.04)` 2px/6px grain; light flat **#fbf3df** + `rgba(120,90,40,0.03)` grain · innerModuleFill **n/a** (pickers/footer forced transparent/border:0; textarea on paper) · border `1px color-mix(#d4c89a 36%, var(--panel-border))`; focus `color-mix(#d4c89a 60%, var(--panel-border))`; light `rgba(193,154,77,0.28)` · focusAccent NO glowing ring — focus = border #d4c89a 36%→60% + shadow deepen; tan/gold #d4c89a de-facto accent; gold **#c19a4d** action/send · textPrimary `color-mix(var(--text-primary) 96%, #c19a4d)` warm ink; above-bar `90%`; chips `92%`; light all **#4a3a16** cocoa · placeholder `color-mix(var(--text-secondary) 60%, #c19a4d)` font-style:italic · rimOrGlow no per-shell glow; shared agent-aura rim per above-bar row when .fx-agent-aura (idle `0 0 0 1px rgb(.../.28), 0 18px 58px /.18`; running `/.5 + 0 22px 68px /.24` + pulse; approval warn; failed danger)
**Geometry:** surfaceRadius **14px** (surface + above-bar; ensemble 14px) · controlShape **rounded 4px** (pickers/labels/commands/image-picker/control-button/schedule/runtime) · sendButton **rounded 4px square (NOT circle)**, fill `color-mix(#c19a4d 60%, #fcf6e6)` (solid gold over cream), border `1px color-mix(#c19a4d 72%, transparent)`, glyph **#2a1d05** (near-black warm ink), serif weight 700 (same for run + stop); NO explicit width/height — inherits base sizing · innerRadius n/a · spacing surface padding 14px 18px 12px; textarea padding base 12px 0 6px → padding-top:44px (clears top-right pickers); perforation ::after top:48px → top:90px after padding, left:12px right:12px height:1px
**Controls:** **No per-control order block — raw DOM source order (permission last), NOT the default shell order [corrected — spec wrongly claimed 07:94-99 is global; that block is default-scoped, and no stub order block exists].** Inline-pickers + footer stripped to transparent/border:0. Every chip gets cream treatment: bg `color-mix(#fcf6e6 18%, var(--composer-bg-solid))`, border `1px color-mix(#d4c89a 30%, var(--panel-border))`, radius 4px, color `color-mix(var(--text-primary) 92%, #c19a4d)`, box-shadow:none, serif, font-variant-numeric:tabular-nums. Light swaps to bg #f6ecd1, border rgba(193,154,77,0.32), color #4a3a16. Above-bar trailing/files clusters shared margin-left:auto. No lifted-capsule, no tucked-tab — pickers + send inline on the ticket body.
**Send glyph:** run-triangle
**Typography:** SERIF everywhere `var(--font-serif)` = "New York","Times New Roman",Georgia,serif (theme.css:234) on textarea/above-bar/chips/send/ensemble. Textarea `var(--font-size-md)`. Placeholder italic. Above-bar + chips font-variant-numeric:tabular-nums (monospaced ticket digits). Send weight 700 bold serif. No mono.
**Row policy:** Inside/stacked family — each row (.composer-above-bar, .ensemble-above-row, .queued-messages-above-row, .composer-create-pr-row, compact roster) its own cream ticket-head card sharing the recipe, NOT merged. Ensemble + compact roster mirror cream paper (grain, #d4c89a 36% border, 14px, 0 4px 14px shadow, serif, backdrop-filter:none). Aura matrix: stub applies aura per above-bar ROW; stack-level glow + stack ::after suppressed (box-shadow:none !important; ::after display:none/content:none). Multi-row → one rim per card (rim-chase); single-row likewise. Files cluster floats right margin-left:auto.
**Effects:** GRAIN (always on) — diagonal `repeating-linear-gradient(135deg, rgba(255,248,230,0.04) 0 2px, transparent 2px 6px)` on surface + above-bar (light rgba(120,90,40,0.03)). PERFORATION (`.composer-surface::after`, always on) — content:''; left:12px right:12px height:1px top:48px→90px; `background-image: linear-gradient(to right, color-mix(#d4c89a 56%, transparent) 50%, transparent 50%)`; background-size:8px 1px repeat-x → 4px gold dashes on 8px pitch; opacity:0.62; z-index:1. Only structural pseudo-element unique to stub. BASE SHEEN SUPPRESSED (surface ::before display:none; above-bar ::before background:none). INSET TOP HIGHLIGHT (always on) `inset 0 1px 0 #ffffff@6%` (8% focus). AGENT-AURA RIM (conditional shared) per-row ::after; stack-level force-disabled.
**Theme behavior:** Theme-DERIVED, not immune. Dark/default: cream `color-mix(#fcf6e6 14%, var(--composer-bg-solid))` picks up the bg, ink var(--text-primary) ~90-96% toward #c19a4d. Light/mist/sage flip: surface + above-bar flat #fbf3df + rgba(120,90,40,0.03) grain, border rgba(193,154,77,0.28), text #4a3a16; chips bg #f6ecd1 / border rgba(193,154,77,0.32) / #4a3a16; above-bar descendants forced #f6ecd1 + #4a3a16 text !important. Gold send button NOT re-themed — stays gold #c19a4d / #2a1d05 ink every theme.
**A11y fallbacks:** reduce-motion → only the running agent-aura rim-pulse killed (animation:none + opacity:0); grain/dashes/shadows static unaffected. reduce-transparency → no handling needed (opaque cream, no backdrop-filter; ensemble row hard-sets none). No high-contrast/forced-colors override; SwiftUI should bump border opacity (#d4c89a / rgba(193,154,77,..)), darken text toward #4a3a16, keep gold send.
**Skeptic:** accurate=false; corrections applied = **stub has no per-control order block → DOM source order (permission last), not attach→…→model** (07:94-99 is default-scoped; no stub order block; only claude/codex/cursor/default/gemini/grok/kimi define order). Rest of controls field accurate. Send glyph run-triangle matches global facts. Confidence: high.
**Evidence:** `08-theme-picker-overrides.css:5284-5301` (surface fill/grain/border/shadow/focus), `:5302-5304` (::before display:none), `:5308-5321` (perforation ::after), `:5323-5334` (textarea serif + italic placeholder), `:5337-5350` (above-bar cream + tabular-nums), `:5358-5371` (cream chips r4 serif), `:5373-5381` (gold send #c19a4d r4 #2a1d05 700), `:5383-5403` (light flat #fbf3df/#4a3a16/#f6ecd1), `:6633-6648` (padding-top 44 + perforation top:90), `:6952-6965` (light above-bar !important); `07-composer-shells.css:87-99` (default-scoped order; un-ordered shells DOM order); `09-ensemble-work-session.css:1859-1871`; `05-polish-fx-layouts.css:3030,3051-3052` (stack glow disabled), `:3122-3131,3165-3168,3247-3284`; `theme.css:234` (--font-serif); `App.tsx:20675-20685` (else → RunSymbolIcon).

### satellite — Satellite
**Essence:** "Everything floats" — every container (surface, textarea, above-bar, pickers, footer, combined-picker triggers, ensemble rows, roster picker) is fully transparent/borderless/shadowless/backdrop-filter-free, layout intact. Only labels/icons/text remain as raw floating tokens. The single shaped element is the send button: a 40px accent-filled circle with a colored glow.
**Material:** Transparent/borderless on every container — NOT glass, NOT solid. Surface: bg transparent, border 0, box-shadow none, re-asserted with !important + backdrop-filter none + -webkit none to beat native_glass's (0,4,0) fill; overflow:visible. Textarea fully transparent (no border/radius/box-shadow). Above-bar transparent border 0 no backdrop-filter. ONLY material surface = send button: accent fill `color-mix(var(--accent) 92%, transparent)` + outer glow `0 4px 14px color-mix(var(--accent) 36%, transparent)`, border 0, radius 999px. Dark accent #5a8cff; light #6f93ff. No backdrop-filter anywhere.
**Palette:** surfaceFill **transparent** · innerModuleFill **n/a** (no card-in-card) · border none/transparent everywhere · focusAccent **no focus ring/glow** — :focus-within keeps box-shadow:none + border-color:transparent on surface AND textarea; only accent = send button fill+glow `var(--accent)` = #5a8cff dark / #6f93ff light / data-accent-selectable · textPrimary `var(--text-primary)` = rgba(255,255,255,0.92) D / rgba(18,21,27,0.92) L (textarea full strength) · placeholder inherits base composer-textarea (not overridden) · rimOrGlow send-button glow only `0 4px 14px color-mix(var(--accent) 36%)`; surface/above-bar box-shadow:none; agent-aura ::after rim on above-bar rows when .fx-agent-aura `0 0 0 1px rgb(.../0.28) + 0 18px 58px /0.18` (running 0.5/0.24)
**Geometry:** surfaceRadius **0/none** (transparent; not set); surface padding 6px 4px 0 · controlShape **plain-text tokens, no shape at rest**; pickers/control-buttons transparent border 0 padding 4px 6px; ONLY on hover gain a soft tint pill (`color-mix(var(--text-primary) 6%)` + radius 8px); combined-picker-trigger height 24px padding 0 2px transparent; pickers/footer transparent gap 14px · innerRadius **8px** (hover-only tint pill + roster chips) · sendButton **circle r999px, 40×40px** (base, satellite sets radius 999 explicitly), fill `color-mix(var(--accent) 92%)` ≈ #5a8cff, **glyph color var(--success) #4cc38a green** (base run-btn NOT overridden), 28px run-triangle; stop same accent fill+glow but glyph stays var(--danger) red · spacing textarea padding 8px 2px 10px → padding-top:44px (clears top-right pickers); font-size `calc(--font-size-md + 1px)` min-height 56px; above-bar gap 12px; pickers/footer gap 14px; ensemble rows gap 12px
**Controls:** Default order unchanged (attach, permission, ensemble, provider, workspace, model with model margin-left:auto). Every control flat transparent text/icon: picker-label/-command/image-picker(attach)/control-button/schedule/runtime share bg transparent, border 0, box-shadow none, padding 4px 6px, color `color-mix(var(--text-primary) 78%)`. Hover (not disabled) → full text-primary + 6%-tint pill radius 8px. Combined-picker triggers floating text (transparent, 24px, text-primary 80%, suffix 58%). Ensemble-mode label + fan-out toggle stripped (10:1867-1887) then inherits 78%/hover-tint. Guest-control transparent inline cluster (order 6). Light-mode above-bar children forced transparent text-primary 84%; Create-PR action keeps accent tint `color-mix(var(--accent) 86%, var(--text-primary))`.
**Send glyph:** run-triangle
**Typography:** App sans (no override). Textarea bumped `calc(--font-size-md + 1px)` ≈ 20.14px (base md `calc(1.196rem*1)` ≈ 19.14px). No mono, no serif. Picker text inherits default sizing; roster-preset-picker label letter-spacing 0.06em + color text-primary 52%. No italic placeholder, no tabular-nums. Colors opacity-graded text-primary: 78-80% rest, 100% hover, 58% suffix, 52% roster.
**Row policy:** No `:has()` row-presence gating — every above-bar/ensemble/roster row unconditionally flattened to transparent regardless of count. Stack = bare gap-only wrapper → aura fanned to EACH row: stack-level glow killed, each child row gets position:relative + inset:0 ::after rim/glow border-radius:inherit. Running/approval/failed swap rim color+pulse. HOWEVER satellite re-suppresses the fanned ::after on its ensemble-above-row (09:1795-1802) and roster-preset-picker (09:1834-1841), so only git/changes `.composer-above-bar` + create-pr-row show the aura. Create-PR inherits generic stack-child transparent+aura (no satellite-specific rule). Files-cluster/diff-action margin-left:auto/order 80 (shared block).
**Effects:** Surface ::before DISABLED (display:none; reinforced ::before AND ::after display:none !important). Above-bar ::before/::after background:none then display:none !important + background:none !important. **Send/stop button glow — the ONE visible effect** `0 4px 14px color-mix(var(--accent) 36%)` on accent-fill r999 circle (intrinsic, always on). Hover tint pill on pickers/labels/roster-chips (only :hover:not(:disabled), 6%-text-primary bg radius 8px). Agent-aura ::after rim+glow on above-bar rows (fanned). Ensemble-above-row ::before/::after DISABLED + fanned aura ::after suppressed (no aura on ensemble rows). Roster-preset-picker fanned aura suppressed; save chip keeps 1px dashed border `color-mix(var(--text-primary) 18%)`. send-confirming glyph flourish (base, inherited).
**Theme behavior:** Theme-DERIVED, not immune. Every color a theme token: --text-primary (rgba(255,255,255,0.92)→rgba(18,21,27,0.92)), --accent (#5a8cff→#6f93ff + per-theme #41c7e5/#4fd17d/#ff9b54/#d889ff/#d8a900 + data-accent-selectable blue/purple/pink/orange/green/red/yellow), --success #4cc38a (green glyph), --font-size-md. Light (light/mist/sage): above-bar children transparent text-primary 84%, Create-PR action accent-86%-mixed, roster chips/label rgba(29,29,31,0.84)/0.58, workspace pill legibility fix. No fixed palette — consumes active theme tokens directly.
**A11y fallbacks:** reduce-motion → agent-aura running pulse killed (animation:none + opacity:0); send-confirming flourish governed by global reduce-motion. reduce-transparency → NOTHING shell-specific (zero backdrop-filter, transparent fills; send button accent-fill opaque-ish). No satellite-specific high-contrast; opacity-graded text (52-80%) is the main risk, partly mitigated in light mode; SwiftUI should raise 78-80% toward 100% for high-contrast.
**Skeptic:** accurate=true; corrections applied = none. Non-material only (native_glass (0,4,0) figure echoes a comment; the literal 06:571 rule is empty/(0,3,0) and the real fill is a separate :is() rule; prompt's Create-PR hint at 10:2234 is actually a cursor block, spec correctly says no satellite create-pr rule; textarea px math honest). Send glyph run-triangle matches global facts. Confidence: high.
**Evidence:** `08-theme-picker-overrides.css:5405-5491` (primary block: surface/textarea/above-bar/pickers/label tokens/send button), `:6628-6704` (padding-top 44 + bulletproof transparent !important), `:6967-6986` (light above-bar + Create-PR accent), `:8711-8733` (combined-picker triggers), `:8899-8976` (shared layout); `05-polish-fx-layouts.css:3000-3284` (aura fanned per-row + reduce-motion); `10-provider-shell-overrides.css:1861-1887` (ensemble strip), `:2899-2964` (guest-control); `09-ensemble-work-session.css:1769-1853` (ensemble/roster transparent + aura suppressed + save-chip dashed); `03-composer-welcome-activity.css:6324-6398` (base run-btn glyph var(--success) green 28px NOT overridden); `theme.css:108,148,161,238,273,326,1180-1186`; `App.tsx:20675-20685` (else → RunSymbolIcon); `AppChromeSymbols.tsx:398-413` (RunSymbolIcon).

### obsidian — Obsidian (charcoal rim-shimmer)
**Essence:** Near-black charcoal (#181818) composer whose identity is a crisp 2px WHITE inset rim plus a slow (16s) "lighthouse" shimmer chasing the perimeter via a conic-gradient driven by `@property --obsidian-rim-angle`, masked to the border ring. Ships in a TWO-SURFACE SPLIT: surface goes transparent, and the textarea-wrap + bottom-controls each become their own separately-lit charcoal rect with its own rim-chase; send cluster lifted into the textarea rect's bottom-right.
**Material:** Solid opaque charcoal. Fill **#181818** (`--obsidian-composer-fill`, 08:5554), `background: var(--obsidian-composer-fill) !important` + `backdrop-filter: none !important` (no glass/blur regardless of mode). In the final split form `.composer-surface` becomes `background: transparent !important` no border/shadow (08:6354-6358; **padding:0 at 08:6594-6597 [corrected — not shard 10]**) and the same #181818 fill + 1px border + rim/vignette/glow is re-applied to `.composer-textarea-wrap` AND `.composer-bottom-controls` as two independent lit rects. Above-rows solid #181818 backdrop-filter:none.
**Palette:** surfaceFill **#181818** (`--obsidian-composer-fill`) · innerModuleFill **same #181818** (split rects same tone, separately bordered/lit) · border `rgba(255,255,255,0.10)` (`--obsidian-composer-border`) · focusAccent rim brightens to `rgba(255,255,255,0.42)` (`--obsidian-composer-rim-strong`) + border-color `rgba(255,255,255,0.18)` on :focus-within; send fill `color-mix(var(--accent) 90%, transparent)` · textPrimary `rgba(255,255,255,0.92)` (subtree base 0.86) · placeholder `rgba(255,255,255,0.32)` · rimOrGlow STATIC RIM `inset 0 2px 0 rgba(255,255,255,0.28)`; AMBIENT GLOW `0 0 28px rgba(255,255,255,0.05), 0 0 64px rgba(255,255,255,0.02)`; INNER VIGNETTE `inset 0 -14px 28px -16px rgba(0,0,0,0.85)`; EXTERNAL SHADOW `0 8px 24px -12px rgba(0,0,0,0.7)`; focus glow `0 0 36px rgba(255,255,255,0.07) + 0 0 80px rgba(255,255,255,0.03)`; textSecondary 0.66 / tertiary 0.48 / muted 0.34; controlText 0.74 → #fff hover; diff add #6dd58a, del #ff8a8a
**Geometry:** surfaceRadius **8px** (`--obsidian-composer-radius`); in split lives on the two rects · controlShape **plain text, NOT pills** — all controls flattened (bg transparent, border 0, radius 0, box-shadow none, backdrop-filter none, padding 4px 8px); ensemble-mode active = 1px underline `inset 0 -1px 0 rgba(255,255,255,0.4)` · sendButton **round capsule 40×40px (999px)**, fill `color-mix(var(--accent) 90%, transparent)`, border 0; **glyph 28×28 [corrected — run-btn override 03:6369-6372, not the generic 22×22]** stroke-width 2.2; **glyph color var(--success) GREEN [corrected — inherits base run-btn, obsidian sets no color]** (stop var(--danger) red) · innerRadius 8px each rect; rim-chase ::before ring radius = 8+2 = 10px · spacing split: surface padding 0; textarea-wrap padding 0 6px 2px; bottom-controls flex column gap var(--space-sm) padding 6px 8px; send cluster abs `bottom: calc(100% + var(--space-sm) + 10px) right:14px z-index:4` (lifted into textarea rect bottom-right); textarea padding-top 12px z-index 2; above-rows radius 8px
**Controls:** Bottom-controls forced vertical flex column (gap var(--space-sm) padding 6px 8px). Row-1 (.composer-inline-pickers-left) flex:1 overflow:visible, order: attach(1) permission(2) ensemble-mode(3) provider(4, margin-left:auto) model(5). Context control at row's right edge in .composer-inline-actions. Schedule/runtime top-toggles row hidden (display:none). All picker triggers plain floating white text (rgba(255,255,255,0.74)), hover → #fff no background. Roster +/− flattened glyphs (rgba(255,255,255,0.62), hover + → #fff, − → #ff8a8a). Send cluster (display:contents default) → inline-flex absolutely lifted into textarea rect bottom-right (gap 6px). Send button lone capsule.
**Send glyph:** run-triangle
**Typography:** System sans (no override). Textarea weight 400, letter-spacing 0.005em, padding-top 12px. Control text rgba(255,255,255,0.74). No mono/serif/tabular-nums/italic. Placeholder rgba(255,255,255,0.32) normal weight.
**Row policy:** No `:has()` gating WITHIN obsidian's own rules — every detached above-row + compact roster unconditionally inherits charcoal fill + 1px border + rim/glow whenever present, each with its own rim-chase ::before at dimmer opacity 0.78 (vs surface 0.92), 16s in lockstep. Row presence additive: each row = another lit black-rim satellite. The ONLY relevant :has() is external (the 05 aura matrix; and the obsidian APP THEME's `:has(.ensemble-above-row)` codex pairing at **10:804-826 [corrected — not 10:942-944, which is alabaster/codex]** — app-theme, not this composer style). Create-PR gets right-anchored trio reorder (files order8 margin-left:auto, stats order9, action -6px).
**Effects:** RIM-CHASE (surface, 08:5720-5756) `conic-gradient(from var(--obsidian-rim-angle))` transparent 0-150deg → rgba(255,255,255,0.07)@250deg → 0.22@322deg → 0.5@351deg → transparent@360deg (one ~90deg white arc); `mask: linear-gradient content-box + linear-gradient`, `mask-composite:exclude` (-webkit xor); padding=2px (rim-width), inset=-2px, radius=10px; `animation obsidian-rim-chase 16s linear infinite`; z-index:1; opacity 0.92. ON: arc rotates. OFF (reduce-motion): animation:none, frozen at 0deg but visible. STATIC RIM `inset 0 2px 0 rgba(255,255,255,0.28)` (focus 0.42). INNER VIGNETTE `inset 0 -14px 28px -16px rgba(0,0,0,0.85)` (focus softer). AMBIENT OUTER GLOW (focus lifts). TWO-SURFACE-SPLIT ::before — surface ::before content:none; textarea-wrap::before AND bottom-controls::before each carry identical rim-chase (16s, opacity 0.92, inset -2px, radius 10px) chasing independently. ABOVE-ROW rim-chase ::before dimmer stops (0.06/0.2/0.44) opacity 0.78 + static rim + lighter glow. SEND-BUTTON rim `inset 0 1px 0 rgba(255,255,255,0.32) + 0 6px 18px color-mix(var(--accent) 32%) + 0 0 16px rgba(255,255,255,0.06)`. ENSEMBLE-MODE active underline.
**Theme behavior:** THEME-IMMUNE — locks DARK palette regardless of app theme. Doubled-attribute `[data-composer-style="obsidian"][data-composer-style="obsidian"]` scopes dark tokens (--text-primary 0.92 down to --text-muted 0.34; --panel-bg/--composer-bg/--surface-1/2 all → #181818; --panel-border → obsidian border) onto the whole composer subtree + color-scheme:dark. Light-theme hardcoded selectors (above-bar branch/files/stats/action) force-overridden transparent + rgba(255,255,255,0.88) text + 0.10 border !important. In LIGHT app themes it still renders charcoal-with-white-rim (does NOT flip). NOTE: `[data-theme="obsidian"]` rules in shard 10 are the separate Obsidian APP THEME decorating OTHER styles (codex/claude/gemini/kimi) — not this composer style.
**A11y fallbacks:** reduce-motion → ALL rim-chase animations stop (surface, above-row, both split rects); arc freezes at 0deg; rim/highlight/vignette/glow remain (identity survives motionless). reduce-transparency → no dedicated rule, largely no-op (fill opaque #181818, backdrop forced none; only translucency is decorative rgba layers). forced-colors → no obsidian handling; conic rim + box-shadow rim/glow not guaranteed → likely drop to system colors, only 1px border (0.10) + text remain; SwiftUI: treat rim/glow/chase as degradable, keep solid border + text contrast as the high-contrast floor.
**Skeptic:** accurate=false; corrections applied = (1) **send glyph color var(--success) GREEN, not white** (inherits base run-btn 03:6379-6390; obsidian sets no color); (2) **send glyph 28×28, not 22×22** (run-btn override 03:6369-6372); (3) surface padding:0 is at **08:6594-6597, not shard 10** (shard 10 is only 3539 lines); (4) obsidian/codex `:has(.ensemble-above-row)` is at **10:804-826, not 10:942-944**. Send glyph run-triangle matches global facts. Confidence: high. (Note: stale source comments say #000/1px/12s but live tokens are #181818/2px/16s — spec correctly uses live values.)
**Evidence:** `08-theme-picker-overrides.css:5528-5532` (@property --obsidian-rim-angle), `:5553-5572` (tokens #181818/rim/glow/16s), `:5594-5626` (theme-immune doubled-attr), `:5671-5705` (surface solid + rim + focus), `:5720-5772` (surface ::before rim-chase + keyframes), `:5808-5872` (above-rows + per-row chase), `:5927-6001` (controls flattened + roster +/−), `:6006-6015` (send capsule + rim), `:6354-6430` (TWO-SURFACE SPLIT), `:6531-6597` (control order + send lift + top-toggles hidden + surface padding 0); `03-composer-welcome-activity.css:6369-6390` (run-btn glyph 28px + var(--success)/var(--danger)); `10-provider-shell-overrides.css:804-826` (obsidian/codex :has app-theme); `App.tsx:20675-20685`.

### alabaster — Alabaster (cream, charcoal rim-shimmer)
**Essence:** Polar inverse of Obsidian — warm cream (#f4f3ef) composer, no glass/blur, soft warm ambient glow, ringed by an animated CHARCOAL rim-shimmer (16s). Theme-immune (hard-locks light charcoal-on-cream). Uses the two-surface split (shared with Obsidian): surface transparent, textarea-wrap + bottom-controls each an independent lit cream rect with synced charcoal rim-chase; controls flattened to floating charcoal text; send cluster lifted into the textarea rect's bottom-right.
**Material:** Solid opaque cream — NOT glass. Fill `var(--alabaster-composer-fill)` = **#f4f3ef !important** on every surface. `backdrop-filter` AND `-webkit-backdrop-filter` forced none !important on surface + all rows (08:6114-6115, 6200-6201, 6272-6273) → transparency-reduction no-op. `color-scheme:light`. Only translucency = soft ambient glow + inset rim hairlines (rgba over opaque cream). No separate dark fill — always renders cream LIGHT regardless of theme.
**Palette:** surfaceFill **#f4f3ef** (`--alabaster-composer-fill`, applied !important) · innerModuleFill **same #f4f3ef** on both split rects · border `rgba(18,21,27,0.12)` (`--alabaster-composer-border`) · focusAccent border-color `rgba(18,21,27,0.22)` on :focus-within; rim hairline strengthens to `--alabaster-composer-rim-strong` = `rgba(18,21,27,0.34)` · textPrimary `rgba(18,21,27,0.92)` (subtree base color also `rgba(18,21,27,0.86)`) · placeholder `rgba(18,21,27,0.42)` · rimOrGlow charcoal inset top-rim `inset 0 2px 0 rgba(18,21,27,0.20)` (`--alabaster-composer-rim`); warm static glow `0 0 28px rgba(180,160,120,0.06), 0 0 64px rgba(180,160,120,0.03)`; warm drop `0 8px 24px -12px rgba(60,50,30,0.12)` + bottom inner-shade `inset 0 -14px 28px -16px rgba(0,0,0,0.06)`; focus adds `0 0 36px rgba(180,160,120,0.08), 0 0 80px rgba(180,160,120,0.04)`; plus the animated charcoal conic chase; textSecondary 0.62 / tertiary 0.45 / muted 0.32; controlText `rgba(18,21,27,0.74)` → #0a0a0a hover; diffAdd #2a8a4a, diffDel #c43a3a
**Geometry:** surfaceRadius **8px** (`--alabaster-composer-radius`; rows + each split rect 8px) · controlShape **plain-text / no shape** — all controls flattened (bg transparent, background-image none, border 0, radius 0, box-shadow none, padding 4px 8px) → floating charcoal labels · sendButton **circular pill (999px)**, border 0, fill `color-mix(var(--accent) 90%, transparent)`, shadow `inset 0 1px 0 rgba(255,255,255,0.42)` (bright top highlight) + `0 6px 18px color-mix(var(--accent) 22%)` + `0 0 16px rgba(0,0,0,0.04)`; size/glyph color inherited from base (run-triangle, glyph var(--success) green; rule also targets stop-btn) · innerRadius 8px each rect; rim-chase ::before radius 8+2 = 10px · spacing surface padding 0 (both rects full width); bottom-controls flex column gap var(--space-sm) padding 6px 8px; textarea-wrap padding 0 6px 2px; textarea padding-top 12px; inner-pickers-left flex:1 overflow:visible; send cluster abs `bottom: calc(100% + var(--space-sm) + 10px) right:14px z-index:4 gap 6px` (anchored to position:relative bottom-controls)
**Controls:** FLATTENED to floating charcoal text — every control + ensemble-mode-button loses chrome (transparent, border 0, radius 0, no shadow, padding 4px 8px, color rgba(18,21,27,0.74)). Hover → #0a0a0a. Active ensemble-mode tab = underline `inset 0 -1px 0 rgba(18,21,27,0.5)`. Roster +/− flattened glyphs (rgba(18,21,27,0.56), hover + → #0a0a0a, − → #c43a3a). Order via flex order: attach(1) permission(2) ensemble-mode(3) provider(4, margin-left:auto) model(5). Schedule/runtime top-toggles row hidden (display:none). Send cluster (display:contents default) → inline-flex lifted into textarea rect bottom-right. Branch chip suppressed in JSX (App.tsx:15610-15613). No satellite/telemetry recoloring beyond locked-token inheritance.
**Send glyph:** run-triangle
**Typography:** System sans (no override). Textarea weight 400, letter-spacing 0.005em, charcoal rgba(18,21,27,0.92), z-index 2. Placeholder rgba(18,21,27,0.42), no italic specified. Control labels plain-weight charcoal. No mono, no tabular-nums, no serif.
**Row policy:** Detached above-rows + compact roster each independently get the full cream treatment (#f4f3ef !important, 1px charcoal border, 8px, charcoal inset top-rim + warm shadow/glow, own charcoal rim-chase ::before). Multi-row → every row its own lit cream tile with own chasing rim (no merged-instrument). Aura matrix: alabaster in per-row group — stack-level glow killed (box-shadow none !important, stack ::after display:none), each child row gets ::after aura ring `0 0 0 1px rgb(.../0.28) + 0 18px 58px /0.18`, :has()-gated by .fx-agent-aura. `.composer-bottom-controls` (display:contents default) promoted to flex container only for obsidian/alabaster as part of the split.
**Effects:** Surface rim-chase ::before — inset -2px, radius 10px, padding 2px, border-mask trick (mask-composite:exclude/xor) → 2px ring. Fill CHARCOAL `conic-gradient(from var(--obsidian-rim-angle))`: transparent 0-150deg, rgba(0,0,0,0.06)@250deg, rgba(0,0,0,0.2)@322deg, rgba(0,0,0,0.45)@351deg, transparent@360deg. `animation obsidian-rim-chase var(--alabaster-rim-chase-duration)=16s linear infinite`. opacity 0.78. Reuses SHARED @property --obsidian-rim-angle + @keyframes obsidian-rim-chase. TWO-SURFACE SPLIT — surface transparent !important, ::before cancelled (content:none); textarea-wrap AND bottom-controls each #f4f3ef + 1px charcoal + 8px + inset 0 2px 0 charcoal rim + inset 0 -14px 28px -16px rgba(0,0,0,0.06) + 0 8px 24px -12px rgba(60,50,30,0.12) + warm glow, each its OWN rim-chase ::before (16s opacity 0.78) driven by the one shared angle → lockstep. Per-row rim-chase ::before (softer arc rgba(0,0,0,0.05/0.18/0.4), opacity 0.68, radius 10px). Static charcoal inset top-rim hairline on surface + every row. Warm ambient glow (`rgba(180,160,120)` blurs). Send button bright inner top-highlight inset 0 1px 0 rgba(255,255,255,0.42). focus-within → rim 0.34, border 0.22, warm glow intensifies (transition 220ms). Body-portaled popovers (.shell-alabaster) inherit cream (#f4f3ef bg, charcoal border, color-scheme light, shadow `0 18px 44px rgba(60,50,30,0.16) + inset 0 1px 0 rgba(255,255,255,0.6)`; also overrides --text-tertiary/--text-muted to rgba(18,21,27,0.5) + --surface-text 0.92).
**Theme behavior:** THEME-IMMUNE. Doubled-attribute `[data-composer-style="alabaster"][data-composer-style="alabaster"]` locks LIGHT tokens for the whole subtree: charcoal text rgba(18,21,27,.92/.62/.45/.32), all panel/composer/surface/glass bg → #f4f3ef, borders → rgba(18,21,27,0.12), --provider-grok-color charcoal + base color rgba(18,21,27,0.86), color-scheme:light. Dark-theme hardcoded selectors (above-bar branch/files/stats/action) overridden transparent + charcoal text !important + border-color rgba(18,21,27,0.10) !important. Always reads cream LIGHT; no dark variant. (Separately the `[data-theme="alabaster"]` SYSTEM theme styles codex/claude/gemini/kimi **+ cursor + grok [corrected — six shells]** provider shells, in **shard 10 ONLY (from :534) [corrected — no such rules in shard 07]** — a different axis.)
**A11y fallbacks:** reduce-motion → rim-chase disabled on ALL three carriers via `@media (prefers-reduced-motion: reduce)` (surface ::before, per-row set, split rects); charcoal ring stays but freezes at 0deg → static arc in the **upper-left/left region [corrected — conic `from 0deg` sweeps clockwise from 12 o'clock; arc at 250-351deg sits upper-left, NOT lower-right]**. reduce-transparency → no-op (no glass, backdrop forced none, fill opaque #f4f3ef). forced-colors/prefers-contrast → no alabaster handling; SwiftUI must synthesize high-contrast (darken border to rim-strong rgba(18,21,27,0.34), drop warm glow).
**Skeptic:** accurate=true (corrections are peripheral/non-shell); corrections applied = (1) `[data-theme="alabaster"]` system theme covers SIX shells (adds cursor + grok) in shard 10 only — not "four shells in 07/10"; (2) reduce-motion frozen arc sits upper-left/left, not lower-right. Both are asides outside the core shell. Send glyph run-triangle matches global facts. Confidence: high. (Omissions: send rule also targets stop-btn; token-lock sets subtree base 0.86 + provider-grok-color; dark-selector override also sets border 0.10; popover block also overrides text-tertiary/muted.)
**Evidence:** `08-theme-picker-overrides.css:6040-6053` (tokens #f4f3ef/rim/16s), `:6056-6086` (theme-immune doubled-attr light lock), `:6111-6134` (surface cream + rim + focus), `:6140-6173` (surface rim-chase ::before + reduce-motion), `:6175-6187` (textarea + placeholder), `:6192-6250` (detached rows + per-row chase), `:6256-6314` (flatten controls + roster +/−), `:6318-6327` (send pill accent + inner highlight), `:6432-6509` (TWO-SURFACE SPLIT), `:6531-6597` (control order + send lift + top-toggles + padding 0), `:5528-5532 + 5768-5772` (shared @property + keyframes), `:8413-8435` (.shell-alabaster popover); `05-polish-fx-layouts.css:3011-3131` (per-row aura); `10-provider-shell-overrides.css:534+` (alabaster system theme, six shells); `App.tsx:15610-15613` (branch chip suppressed), `:20675-20685` (else → RunSymbolIcon).

### Cross-cutting notes
- **Theme-IMMUNE styles (lock their own palette regardless of app theme):** **claude** (self-locking white-on-#1e1e1e in dark; only "+" glyph + welcome cards flip in light), **gemini** (immune at the capsule level — navy by default; explicit near-white repaints for light/mist/sage + alabaster), **kimi** (immune at core — hardcoded #0a0a0c + olive; re-keys only for light/obsidian/alabaster), **cursor** (immune BOTH directions but bidirectional gray — flips gray-on-dark vs dark-on-gray, never chroma at rest), **grok** (immune + bidirectional monochrome — white-on-black ↔ black-on-white), **terminal** (immune — forces dark green even under light themes via !important + color-scheme:dark), **obsidian** (immune — locks dark charcoal via doubled-attribute, does not flip in light), **alabaster** (immune — locks light cream via doubled-attribute, no dark variant). Theme-DERIVED (re-tint with the app theme): **default**, **codex** (hybrid — locks dark, derives in light), **modular**, **stub**, **satellite**.
- **Two-surface split (outer surface emptied; a second inner rect carries the fill/chrome):** **default** (glass bezel + opaque inner module — the only one where the two layers are *different tones/materials*), **codex** (#212121 outer + #252525 inner card-in-card), **gemini** (transparent surface + solid navy capsule `.composer-textarea-wrap`), **cursor** (transparent surface + solid gray capsule), **obsidian** (transparent surface + textarea-wrap AND bottom-controls each a lit charcoal rect), **alabaster** (transparent surface + textarea-wrap AND bottom-controls each a lit cream rect). (claude is adjacent — surface emptied, chrome re-homed onto the *textarea* itself, but it's a single bubble not a paired split.)
- **Float above-rows ABOVE the stack (detached pills, not merged into the instrument):** **codex** and **cursor** only — driven by `aboveRowsFloatAboveStack = composerStyle==='cursor' || 'codex'` (App.tsx:18185-18186); the git/Create-PR primary + secondary-workspace rows render as detached pills above the merged stack. (grok merges or tucks via its own `:has()` solo-vs-merged switch; default merges via `:has()`; all others stack as independent rows.)
- **Skeptic returned accurate=false (double-check these styles):** **default**, **codex**, **claude** (most corrections — its entire 08 last-writer layer overturns ~9 headline claims: bubble fill #1e1e1e, border 0.18, permission is a contained button with no orange, min-height 56px, chips display:none, files/stats solid #3a3a3a, tabular-nums present, focus 30%), **cursor** (materially wrong on agent-aura — the merged stack DOES glow chroma during activity), **grok** (transcript backdrop-filter overstated), **modular** (light-variant shadows), **terminal** (font-size scale `*1` not `*1.17`; no control-order block), **stub** (no control-order block → DOM order), **obsidian** (send glyph is green 28px, not white 22px; two shard-10 line-number slips). accurate=true: **gemini**, **kimi**, **satellite**, **alabaster** (alabaster's two corrections are peripheral non-shell asides).
- **Styles requiring non-sans fonts:** **terminal** = MONOSPACE everywhere (`var(--font-mono)` = "SF Mono"/"JetBrains Mono"/"Cascadia Code"/Menlo/Monaco/"Courier New"/ui-monospace); **stub** = SERIF everywhere (`var(--font-serif)` = "New York"/"Times New Roman"/Georgia). All other 11 styles are system sans. (tabular-nums appears in **stub** (above-bar + chips) and **claude** (above-bar files/stats) — note for digit alignment even though both are otherwise font-family-specific only for stub.)


---

## Part D — `:has()` row-presence & pseudo-element matrix

### Row-presence (`:has()`)
| Style | Rule | Effect |
|---|---|---|
| **default** | `.composer-area:has(.composer-above-bar-stack)` (03:3240-3303) | **Merged instrument** — stack + surface fuse into one segmented frame (stack rounded top, surface squared top + 1px divider; running-pulse rim stays on the surface). No rows → standalone rounded surface. |
| **grok** | `.composer-above-bar-stack:not(:has(> :nth-child(2)))` vs `:has(> :nth-child(2))` (10:1427 / 1453-1516) | **Tucked-tab switch** — solo row → one tucked tab (top-only radius, narrowed, centered); ≥2 rows → merged frame w/ hairline dividers. |
| **cursor / codex** | `aboveRowsFloatAboveStack` (App.tsx:18185-18186) | git/Create-PR + secondary-workspace rows **float ABOVE** the stack (detached), not inside it. |
| all 13 | aura matrix (05:3020-3280) | glow/halo on `.composer-above-bar-stack` toggled by solo vs ensemble-row presence. |
| shell-agnostic | `.composer-inline-pickers:has(.composer-inline-actions .stop-btn)` (03:3874-3879) | re-lays the picker row while a run is active. |

### Pseudo-elements (visible chrome)
| Style | Pseudo | Draws |
|---|---|---|
| default | (none style-specific) | merged frame = real borders, not a pseudo |
| codex | `.composer-above-bar::before`; `[data-composer-control]::after` ×4 (07:141/177/263/316) | tab edge; **dropdown carets**; lightning glyph before model |
| claude | `.composer-surface::before` + `.composer-above-bar::before` | subtle rims |
| gemini | `.composer-surface::before{content:none}` (07:2182); `model::after` | cancels base edge; model caret |
| kimi | `.composer-surface::before{display:none}` (07:2581); `model::after` | suppresses blue edge; model caret |
| grok | `model::after` (+hover); tucked-tab rim accents | caret + rim |
| cursor | `.composer-surface::before` rim; `model::after` | flat-gray rim; caret |
| modular | `.composer-surface::before{display:none}`, `.composer-above-bar::before{background:none}` | strips container chrome so pills float |
| terminal | `.composer-surface::before` = **`>` caret** (08:4972-4988); chip `span::before "["` + `::after "]"` (08:5085-5098) | command-line caret + **bracket chips** |
| stub | `.composer-surface::after` = **dashed perforation** at `top:48px` (08:5308-5321) | ticket perforation (paper grain is on the element `background`) |
| satellite | `::before`/`::after` mostly **suppress** chrome | transparency |
| obsidian | `.composer-surface::before` rim-chase conic (08:5720); per-row `::before` chase; post-split surface `::before{content:none}` + `.composer-textarea-wrap::before` + `.composer-bottom-controls::before` | **white rim-shimmer chase** ring per lit rect |
| alabaster | charcoal mirror of obsidian (08:6140) | **charcoal rim-shimmer chase** |

### Two-surface split
ONLY **obsidian** + **alabaster** restructure rows: `.composer-surface` → transparent; `.composer-textarea-wrap` + `.composer-bottom-controls` each become a separately-lit rect with its own animated rim-chase (`.composer-bottom-controls` flips `display:contents` → `display:flex` column). All other 11 shells keep `.composer-bottom-controls` as `display:contents`.

---

## Part E — iOS architecture & implementation plan

### E.1 — Zero desktop changes required
The Mac **already broadcasts** `composerStyle` to iOS:
- **Payload** `RemoteShellAppearance` (`RemoteTaskProjection.ts:375-390`):
  `{ schemaVersion, generatedAt, appearanceMode, visualEffectStyle, themeAppearance, themeCornerStyle, themeAccentStyle, promptSurfaceStyle, composerStyle, reduceTransparency, reduceMotion, compactDensity, preferredColorScheme, colors }`.
- Built by `buildRemoteShellAppearance(AppStore.getSettings())` and pushed **first** in
  `listRemoteProjectionEnvelopes()` with stable `envelopeId:'remote-shell-appearance:global'` (`index.ts:17251-17258`).
- Bundled into `RemoteProjectionSnapshot{projections:[]}` by `broadcastRemoteProjectionSnapshot()`
  (`BridgeBroadcaster.ts:444-450`), re-broadcast on every settings change (`index.ts:946-947`) + ~30
  other state transitions. So a Mac `composerStyle` change reaches the phone in the next snapshot.
- iOS **drops it today**: both `applySnapshot` (`RemoteSessionModel.swift:1872-1954`) and
  `merge(envelope:)` (`:1171-1206`) `switch` on `envelope.kind` and hit `default: break`.
  **Adding one `case "shellAppearance"` to each is the entire decode.**
- The iOS `RemoteProjectionEnvelope` (`Models.swift:143-156`) omits the top-level `generatedAt`,
  so **stale-gating keys off the PAYLOAD's `generatedAt`** (`RemoteShellAppearance.generatedAt`).

### E.2 — Target split (dictated by `Package.swift`)
- **`TaskWraithKit`** — pure (no SwiftUI/UIKit); the **only** target `TaskWraithKitTests` depends on → all testable logic.
- **`TaskWraithUI`** — SwiftUI, depends on Kit; compile-checked by `swift build` (no unit tests) → views/recipes/wiring.
- Tests = **Swift Testing** (`import Testing`, `@Suite`/`@Test`) in `ios/TaskWraithKit/Tests/TaskWraithKitTests/`. There is **no** SwiftUI preview/snapshot harness today — the fixture gallery (CS3) is the visual-QA surface.

**File plan:**
- **Kit:** `ComposerShellStyle.swift` (`TWComposerStyle`), `RemoteShellAppearance.swift`
  (`TWRemoteShellAppearance` Codable + staleness helper), `ComposerShellPreference.swift`
  (`TWComposerShellPreference` + precedence resolver). Tests: `ComposerShellResolverTests.swift`.
- **UI:** `ComposerShellRecipe.swift` (`ResolvedComposerShell` + recipe table),
  `ComposerShellResolver.swift` (style+context→resolved), `ComposerShellContainer.swift`,
  `ComposerShellFixtureView.swift`; edits to `RemoteSessionModel.swift` (decode), `Theme.swift`
  (`TWThemeStore` preference), and the composer hosts.

### E.3 — Style model
`TWComposerStyle`: 13 known cases with raw `String` values (e.g. `case defaultShell = "default"`);
`init(raw:)` maps an unknown string to `.unknown(String)` that **preserves the raw value for
diagnostics** but resolves to `.default` for visuals. Pure, in Kit.

### E.4 — Preference + precedence
- `TWComposerShellPreference` = `.followMac` (default) | `.override(TWComposerStyle)`. Persist **only
  the mode/value** (raw string under `tw.composerShell` on `UserDefaults`, mirroring `TWThemeStore`;
  bump `revision`). **No iOS→Mac writeback. NEVER persist the effective style.**
- Precedence (pure fn, Kit): `effectiveComposerStyle(preference, projected)` = local override →
  Mac-projected `composerStyle` → `.default`.
- Staleness: apply an incoming `shellAppearance` only if its payload `generatedAt` is **newer** than
  the last applied (ignore stale/equal). Pure helper, unit-tested.

### E.5 — Resolver boundary (two distinct resolvers — do not conflate)
- **Kit (pure, testable):** style **precedence** → a `TWComposerStyle`.
- **UI (SwiftUI):** visual **recipe** → `resolve(style, context) → ResolvedComposerShell`
  (palette/geometry/typography/controls/rowPolicy/effects). Context = app theme, run state, row
  presence, presentation mode (`main`/`welcome`/`miniSideChat`), size/width, Dynamic Type,
  reduce-motion, reduce-transparency, contrast, keyboard/safe-area.

### E.6 — Shared-builder refactor
The shell assembly is **duplicated**: `ThreadDetailViews.composerShellStack` (`:778`) and
`MiniThreadView.composerShell` (`TWSharedViews.swift:5682`). CS4 factors it into **one** recipe-driven
builder so per-style logic applies to both; the mini composer gets its own **compact** recipe path
based on measured width (not inherited main-composer geometry).

### E.7 — Surfaces needing per-style treatment
Main composer (`ThreadDetailViews:778`), mini/side-chat composer (`TWSharedViews:5682`),
empty/welcome canvas (`ThreadDetailViews:1084` / `WelcomeViews`), ensemble roster (`EnsembleViews:19`
/ `EditableRosterStrip TWSharedViews:3582`), queued rows (`TWSharedViews:4909`/`5030`), attachment
chips (`ComposerView:323-349`), telemetry rail (`TelemetryFooterRail TWSharedViews:3019`), shell glass
(`ComposerShellGlassModifier TWSharedViews:321`). Theme tokens via `TWTheme` (`Theme.swift:14`). The
`default` shell currently hardcodes `cornerRadius 16` + literals — these become recipe-driven, and the
default recipe **must reproduce today's look 1:1** (it is signed off).

### E.8 — Slice plan & gates
CS1 inventory (this doc) · CS2 plumbing · CS3 recipes + fixture · CS4 core body + shared builder ·
CS5 rows/welcome/telemetry · CS6 mini composer + popovers + premium effects + a11y · CS7 settings
picker · CS8 validation + docs.
**Gates:** per slice `cd ios/TaskWraithKit && swift build` (+ `swift test` where Kit logic changed);
CS8 adds `xcodebuild -project ios/TaskWraithApp/TaskWraith.xcodeproj -scheme TaskWraith -destination
'platform=iOS Simulator,name=iPhone 16' build` / `test`, contact-sheet QA of all 13 via the fixture
gallery, and the a11y matrix. **No repo-wide formatting; do not touch dirty desktop CSS/settings
files** (concurrent sessions edit this tree).

### E.9 — Out of scope / keep-list
- **Out:** full `data-interface-style` app chrome; deep popover styling beyond what's feasible;
  iOS→Mac writeback; persisting the effective style.
- **Keep global:** do NOT add composer style to Composer `@State`, thread/task card models, remote
  action payloads, or `SceneStorage`.
- **`default` parity is sacrosanct** — its recipe reproduces the shipped look exactly.

## Part F — CS10: structural LAYOUT parity (build 11)

CS1–CS8 ported per-component *styling* (material/palette/geometry/effects) but every
shell still shared the native *arrangement* — switching shells changed colors, not the
composer layout. CS10 adds the structural layer.

### F.1 — Contract
`ComposerShellLayout` (ComposerShellRecipe.swift) — 6 bool flags on `ResolvedComposerShell`,
all-false `.standard` = the signed-off native arrangement:
- `controlsBelowTextarea` — control row renders BELOW the text input (vs above + hairline).
- `detachedAboveRows` — each above-row (changes/workspace/queued/roster) + the composer
  core become their OWN `.composerShell` card with gaps, vs one merged surface with hairlines.
- `liftedSend` — send floats to the input corner (iOS already places send at the input
  trailing edge, so this is effectively a no-op on iOS).
- `surfaceIsCapsule` — input surface is a single capsule (gemini/cursor).
- `splitChromeRects` — textarea + controls each own a separate lit rect (obsidian/alabaster).
- `controlsAsPlainText` — control chips flatten to bare text (the iOS model picker is
  already flat plain-text for all shells, so this is largely a no-op on iOS).

### F.2 — Resolution (central)
`ComposerShellResolver.composerLayout(for:)` — one switch maps every style → its layout,
applied in `resolve()` (`base.layout = composerLayout(for: style.renderStyle)`). Single
source of truth; recipes no longer carry layout inline.

### F.3 — Verified flag table
Derived by a 22-agent extract+adversarial-refute pass over the desktop CSS
(`07-composer-shells.css`, `05-polish-fx-layouts.css`, `10-provider-shell-overrides.css`)
+ the light/dark screenshot matrix in `design-assets/TaskWraith Composer Shell Electron
Variants/` (zero refute-corrections, all high-confidence):

| shell | ctrlsBelow | detached | lifted | capsule | splitRects | plainText |
|-------|:--:|:--:|:--:|:--:|:--:|:--:|
| default | · | · | · | · | · | · |
| codex | ✓ | ✓ | · | · | · | · |
| claude | ✓ | ✓ | ✓ | · | · | · |
| cursor | ✓ | ✓ | ✓ | ✓ | · | ✓ |
| grok | ✓ | · | · | · | · | · |
| gemini | ✓ | ✓ | ✓ | ✓ | · | · |
| kimi | ✓ | ✓ | · | · | · | · |
| modular | ✓ | ✓ | · | · | · | · |
| terminal | ✓ | ✓ | · | · | · | · |
| stub | ✓ | ✓ | · | · | · | · |
| satellite | ✓ | ✓ | · | · | · | ✓ |
| obsidian | ✓ | ✓ | ✓ | · | ✓ | ✓ |
| alabaster | ✓ | ✓ | ✓ | · | ✓ | ✓ |

Two clean rules: `controlsBelowTextarea` = every non-default shell; `detachedAboveRows` =
all but grok (grok tucks its above-rows behind the composer lip).

### F.4 — Host seams
- `ComposerView.body` branches on `shell.layout.controlsBelowTextarea` (input-first vs
  controls-first; the control row stays hidden for ensembles in both branches).
- `composerShellStack` (ThreadDetailViews) branches on `detachedAboveRows` via the
  `composerShellIf(apply:)` helper (ComposerShellContainer.swift) — `apply:false` returns
  the view untouched, so the merged/default path is byte-identical.

### F.5 — Build scope
**Build 11 (CS10):** `controlsBelowTextarea` (11 shells) + `detachedAboveRows` (10 shells),
plus the gemini surface-radius fix (its `.solid` navy capsule was drawn at radius 0 → square
detached pills; now 26px to match the capsule). `liftedSend` + `controlsAsPlainText` are
effectively already satisfied on iOS (trailing-edge send; flat plain-text model picker).

**Build 12 (CS11 A+B):** codex roster-tuck (`tucksSecondaryRows` — roster/queued rows tuck
into the core card while changes/PR pill out) + the Reduce-Transparency double-fill fix
(`onOwnCard` suppresses a detached row's own opaque fill). 3-agent review: default
byte-parity PASS, both fixes PASS.

### F.6 — Verification
- `swift build` + 69 Kit tests + a full `xcodebuild` simulator app build, per slice.
- A 15-agent adversarial review (12 per-shell parity vs the light/dark matrix + 3 skeptics)
  graded default byte-parity **PASS** (traced element-by-element + git-diff confirmed),
  layout values/wiring **PASS** (all 13 cases match the table), detached-restructure
  **CONCERN** (no ViewBuilder/chaining defects; the fill issues in F.7). All 12 non-default
  shells: layout value correct, consumed-flag structural match **full**.

### F.7 — Done in build 12 (CS11 A+B)
- **codex roster-tuck** — `tucksSecondaryRows`: codex's roster/queued rows tuck into the
  core card as merged segments while changes/PR rows pill out (desktop
  `:not(:has(.ensemble-above-row))`). `composerSecondaryRows(onOwnCards:)` keeps non-codex
  shells byte-identical.
- **Reduce-Transparency double-fill** — `onOwnCard` suppresses a detached row's own opaque
  `surface1` fill so the per-card `.composerShell` surface shows (a11y-mode only; glass-on
  was already clean).

### F.8 — Done in CS12 (input-owns-surface / bottom-row parity)
The bottom rows (control row + telemetry) fused into ONE container for solid shells; desktop
frames only the input and floats the footer bare/separate. Fixed via `surfaceWrapsInputOnly`
+ the computed `inputOwnsSurface` (= surfaceWrapsInputOnly || surfaceIsCapsule ||
splitChromeRects): the shell surface wraps ONLY the input (composerInputBody.composerShellIf),
the core card is no longer wrapped, and the footer is bare (claude), its own rect
(obsidian/alabaster splitChromeRects), or under a capsule input (gemini/cursor
surfaceIsCapsule; cursor radius 26). codex fuses its control row into the #252525 inner
module; satellite/modular drop the stray divider; a 6px gap separates the framed input from
the bare footer. default/grok/kimi/terminal/stub stay ONE merged surface — 3-agent
re-verified byte-identical.

### F.9 — Done in CS12c
- **controlsAsPlainText** (cursor/satellite/obsidian/alabaster) — the approval token now
  flattens to bare text (`controlChipFill`) on the bare footer, matching the flat model
  picker.
- **liftedSend** — effectively satisfied on iOS: the 1–2 line input keeps the send at its
  trailing edge (= the corner); the desktop "lift" only matters for a tall growing textarea.

### F.10 — Done in CS13 (build-14 screenshot review)
- **codex above-row rim** — `codexRecipe.palette.rim` = rgb(87 148 255) (ring 0.28 / glow
  0.18); the detached Create-PR / changes / secondary-workspace pills now carry the desktop
  agent-aura rim (it also rims the core, consistent with terminal/stub).
- **claude above-row radius** — `surfaceCornerRadius` 14 → 12 (slightly less round; shared
  with the input bubble; stays in claude's 10-14 band).

### F.11 — Still deferred (live visual review)
- **grok "tucked tabs"** — the above-rows should collapse into ONE narrower (~18px inset),
  top-rounded card that the composer's top edge OVERLAPS by ~10px (a tab peeking from behind),
  with the composer staying one full-width surface. Approach (agent investigation): add a
  `tuckedAboveTab` layout flag (grok only) + a composerShellStack branch wrapping the
  above-rows in one inset `.composerShell` card and the composer-core in another, stacked via
  `VStack(spacing: -10)` (the overlap hides the tab's bottom corners → no top-only-corner
  primitive needed). Deferred: needs the above-rows grouped (they carry many local deps in
  composerShellStack) + visual verification.
- **Theme-immune footer legibility** — cursor/gemini lock a dark palette but the bare footer
  chrome (chips, telemetry) is theme-following → mismatch on a LIGHT app theme; the footer
  should adopt the shell palette for theme-immune shells. (Edge case: light app + locked-dark.)
- **composerLayout value test** (needs the pure layout map moved into TaskWraithKit) +
  **state-identity on live shell switch** (composerShellIf if/else can reset the draft
  mid-compose — LOW).
