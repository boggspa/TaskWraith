# 2026-07-19 multi-provider retro

| Scar | Owner | Status | Durable evidence |
|---|---|---|---|
| Hand-typed same-provider aliases such as `@claude` could select one of two seats nondeterministically | Ensemble routing | Remediated | Main owns exact resolution; ambiguous/stale/disabled targets fail closed in [`EnsembleDmRoutingIntegration.test.ts`](../src/main/EnsembleDmRoutingIntegration.test.ts) and [`EnsembleMentionAlias.test.ts`](../src/main/services/EnsembleMentionAlias.test.ts). |
| Renderer and remote paths had separate seat-routing rules | Ensemble routing | Remediated | Desktop sends only advisory picker identity and main re-resolves the exact participant. See [`mention-yield-routing.md`](../docs/how-to/ensemble-mode/mention-yield-routing.md). |
| Tool aliases and host-vs-broker spellings were hard to discover after a denial | Guest DX | Remediated | Denials name the sanctioned broker route; the stable catalogue is documented in [`Tools.md`](../resources/Tools.md) and provider policy guidance. |
| Headless/scheduled runs could differ from interactive provider posture | Provider runtime | Remediated, externally gated | Provider qualification/canary workflows now exercise the exact posture; sealed *scheduled* Kimi execution remains unavailable until externally commissioned, while interactive Kimi admits as labelled unattested-development. See [`PROVIDER_PERMISSION_CONFORMANCE.md`](../PROVIDER_PERMISSION_CONFORMANCE.md). |
| Large Ensemble panels obscured the useful default topology | Product/renderer | Remediated | Small-panel presets and guidance live in [`saved-roster-presets.md`](../docs/how-to/ensemble-mode/saved-roster-presets.md) and [`POSITIONING.md`](../POSITIONING.md). |
| Review-green execution-graph work could still be live-dead | Runtime verification | Remediated | CI now includes architecture/provider qualification gates and the execution-graph integration suite; live provider claims remain canary-gated. |
| Usage persistence performed synchronous per-record durability work | Store/runtime | Remediated | Append journal plus delayed checkpoint/recovery coverage in [`UsageJournalStore.test.ts`](../src/main/store/UsageJournalStore.test.ts). |
| Solo wakeups persist `cancelOnUserInput`, but no solo user-input cancellation callsite was found | Wakeups | Remediated | Solo sends now cancel pending wakeups at the dispatch seam: `dispatchRunWithProviderPause` in [`index.ts`](../src/main/index.ts) calls `cancelWakeupsOnUserInput` for solo, non-scheduled, non-wakeup-resume payloads; default-`true` regression in [`SoloChatWakeupService.test.ts`](../src/main/SoloChatWakeupService.test.ts). Ensemble wakeups stay on EnsembleOrchestrator. |
| An explicitly approved host rerun after a Codex sandbox/tooling collision can no longer auto-start a same-thread continuation | Codex lifecycle | Remediated | Continuation is a fresh R1 (`createHostRerunContinuationRunId` / correlation mint) that resumes the chat's existing Codex provider session; R0 keeps host projection and independent history-join. Pure helpers in [`HostRerunContinuation.ts`](../src/main/HostRerunContinuation.ts); wire in `continueCodexAfterHostRerun`; coverage in [`HostRerunContinuation.test.ts`](../src/main/HostRerunContinuation.test.ts), [`HostRerunContinuationIntegration.test.ts`](../src/main/HostRerunContinuationIntegration.test.ts), and [`ProviderTransportHistoryIntegration.test.ts`](../src/main/ProviderTransportHistoryIntegration.test.ts). Missing session fails closed (no virgin spawn). |
| Returned sub-thread results persist for an Ensemble parent but never enter a participant seat's provider context | Sub-thread orchestration | Remediated | Authority-seat delivery: join-ready + idle-gated `maybeDrainEnsembleSubThreadMailbox` wakes Boss/Captain via directed `startRound` with `buildSubThreadMailboxContinuationPrompt`. Pure helpers in [`EnsembleSubThreadMailboxDelivery.ts`](../src/main/EnsembleSubThreadMailboxDelivery.ts); wiring + integration in [`EnsembleSubThreadReturnIntegration.test.ts`](../src/main/EnsembleSubThreadReturnIntegration.test.ts). Solo path and projection-only cards unchanged. |
| `SubThreadStatusTicker` exists but is not mounted, so the documented inline active-child strip never appears | Renderer / Guest DX | Remediated | [`SubThreadStatusTicker.tsx`](../src/renderer/src/components/SubThreadStatusTicker.tsx) is mounted above the transcript in the main and side-chat panes ([`MainAppLayout.tsx`](../src/renderer/src/app/views/MainAppLayout.tsx)) and in Multiview panes ([`ChatViewPane.tsx`](../src/renderer/src/components/ChatViewPane.tsx)); integration coverage in [`ChatViewPane.test.tsx`](../src/renderer/src/components/ChatViewPane.test.tsx). The how-to now documents the live ticker. |

**2026-07-26 source-ahead clarification:** the historical “sealed scheduled
Kimi execution remains unavailable” wording above no longer describes current
product policy. Scheduled Kimi remains available and emits an explicit unsealed
receipt until its exact final launch-plan handoff is seal-wired. Seal maturity
is assurance metadata, never provider admission.
