# 2026-07-19 multi-provider retro

| Scar | Owner | Status | Durable evidence |
|---|---|---|---|
| Hand-typed same-provider aliases such as `@claude` could select one of two seats nondeterministically | Ensemble routing | Remediated | Main owns exact resolution; ambiguous/stale/disabled targets fail closed in [`EnsembleDmRoutingIntegration.test.ts`](../src/main/EnsembleDmRoutingIntegration.test.ts) and [`EnsembleMentionAlias.test.ts`](../src/main/services/EnsembleMentionAlias.test.ts). |
| Renderer and remote paths had separate seat-routing rules | Ensemble routing | Remediated | Desktop sends only advisory picker identity and main re-resolves the exact participant. See [`mention-yield-routing.md`](../docs/how-to/ensemble-mode/mention-yield-routing.md). |
| Tool aliases and host-vs-broker spellings were hard to discover after a denial | Guest DX | Remediated | Denials name the sanctioned broker route; the stable catalogue is documented in [`Tools.md`](../resources/Tools.md) and provider policy guidance. |
| Headless/scheduled runs could differ from interactive provider posture | Provider runtime | Remediated, externally gated | Provider qualification/canary workflows now exercise the exact posture; Kimi remains unavailable until externally commissioned. See [`PROVIDER_PERMISSION_CONFORMANCE.md`](../PROVIDER_PERMISSION_CONFORMANCE.md). |
| Large Ensemble panels obscured the useful default topology | Product/renderer | Remediated | Small-panel presets and guidance live in [`saved-roster-presets.md`](../docs/how-to/ensemble-mode/saved-roster-presets.md) and [`POSITIONING.md`](../POSITIONING.md). |
| Review-green execution-graph work could still be live-dead | Runtime verification | Remediated | CI now includes architecture/provider qualification gates and the execution-graph integration suite; live provider claims remain canary-gated. |
| Usage persistence performed synchronous per-record durability work | Store/runtime | Remediated | Append journal plus delayed checkpoint/recovery coverage in [`UsageJournalStore.test.ts`](../src/main/store/UsageJournalStore.test.ts). |
| Solo wakeups persist `cancelOnUserInput`, but no solo user-input cancellation callsite was found | Wakeups | Open | Add a central solo-send cancellation hook and a regression covering the default `true` behavior without affecting Ensemble wakeups. |
| An explicitly approved host rerun after a Codex sandbox/tooling collision can no longer auto-start a same-thread continuation | Codex lifecycle | Open | The rerun result is still projected, but the old continuation reused the original `appRunId` and could not own an independent history-join operation. Allocate a fresh, independently tracked continuation run/turn identity, preserve the approval/output correlation, and add delete-during-continuation coverage before restoring auto-continuation. |
| Returned sub-thread results persist for an Ensemble parent but never enter a participant seat's provider context | Sub-thread orchestration | Open | Solo delivery is mailbox-backed and exactly once, while Ensemble runs bypass mailbox attachment in [`index.ts`](../src/main/index.ts) and filter projection-only tool cards in [`EnsemblePrompt.ts`](../src/main/EnsemblePrompt.ts). Define an authority-safe seat delivery rule and add an Ensemble return integration regression. |
| `SubThreadStatusTicker` exists but is not mounted, so the documented inline active-child strip never appears | Renderer / Guest DX | Open | Mount [`SubThreadStatusTicker.tsx`](../src/renderer/src/components/SubThreadStatusTicker.tsx) in the parent-chat layout and add a render/navigation integration test; until then the how-to marks the surface unavailable. |
