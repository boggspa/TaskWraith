/**
 * Trusted argv contract between the standalone TUI launcher and a Host
 * process. This is intentionally independent of Electron lifecycle policy.
 */
export const TUI_HEADLESS_HOST_ARG = '--taskwraith-headless-host'
export const TUI_HEADLESS_HOST_PARENT_ARG = '--taskwraith-headless-parent='
